---
phase: 08-timeline-progress-bar
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/playback/timelineDensity.ts
  - test/unit/timelineDensity.test.ts
  - src/hooks/usePlaybackEngine.ts
  - src/i18n/translations.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "密度分桶、提交钳制、预览钳制、键盘步进全部是 src/playback/timelineDensity.ts 中的纯函数，在 node Vitest 下全部通过（UI-03, UI-01, D-07, D-08, D-09, D-12）"
    - "usePlaybackEngine 暴露 previewSeek(targetMs)：仅更新引擎位置，不触碰 React session 状态 → 拖拽预览零 IndexedDB 写入（UI-02, D-01, 07-D-09）"
    - "timelineLabel i18n 键在 en 与 zh 两块对称存在，供 Timeline aria-label 使用（UI-01, D-13）"
  artifacts:
    - path: "src/playback/timelineDensity.ts"
      provides: "Timeline 全部可测逻辑：密度分桶 + 钳制 + 键盘步进"
      exports: ["computeCueDensityBuckets", "clampToDuration", "clampPreviewTarget", "computeKeyboardStepTarget", "KEYBOARD_STEP_MS"]
    - path: "test/unit/timelineDensity.test.ts"
      provides: "密度分桶 + 全部钳制函数的边界单测（Wave 0 测试文件）"
      min_lines: 60
    - path: "src/hooks/usePlaybackEngine.ts"
      provides: "engine-only 预览 seek 入口"
      contains: "previewSeek"
    - path: "src/i18n/translations.ts"
      provides: "timelineLabel 键（en + zh 对称）"
      contains: "timelineLabel"
  key_links:
    - from: "test/unit/timelineDensity.test.ts"
      to: "src/playback/timelineDensity.ts"
      via: "相对路径两级向上导入被测函数"
      pattern: "from '\\.\\./\\.\\./src/playback/timelineDensity'"
    - from: "src/hooks/usePlaybackEngine.ts"
      to: "hook 返回对象"
      via: "previewSeek 与既有 seek 并列返回（Plan 02 props 链的源头）"
      pattern: "previewSeek"
---

<objective>
交付 Phase 8 Timeline 的全部非视觉基础（UI-01/UI-02/UI-03）：`src/playback/timelineDensity.ts` 纯函数模块（密度分桶 D-07/D-08/D-09、提交钳制 07-D-02、预览钳制 Pitfall 2、键盘步进 D-12）以 TDD 先行开发；`usePlaybackEngine` 新增 engine-only `previewSeek`（D-01：拖拽预览不逐帧持久化）；`translations.ts` 新增 `timelineLabel` 键（D-13）。

Purpose: Phase 7 已交付全部引擎侧能力；本 plan 把 Timeline 中所有可在 node 环境验证的逻辑抽成纯函数并测试先行（tdd_mode: true、08-VALIDATION.md Wave 0），组件壳（Plan 08-02）保持薄。previewSeek 是双路径 seek（预览/提交）的预览半边——没有它，拖拽预览只能复用既有 seek 而逐帧写 IndexedDB，违反 D-01。
Output: 1 个纯函数模块 + 1 个 Wave 0 测试文件（全绿）+ hook 新入口 + i18n 键；`npx tsc --noEmit` clean、`npm test` 全绿。
</objective>

## Artifacts This Phase Produces

本 phase 创建/修改的全部符号（Plan 08-01 与 08-02 合计，供执行者了解全局契约）：

| 类别 | 符号 | 所在 Plan |
|------|------|-----------|
| 纯函数 | `computeCueDensityBuckets(cues, totalDurationMs, bucketCount): DensityBucket[]` | 08-01 |
| 纯函数 | `clampToDuration(targetMs, totalDurationMs): number`（提交路径 [0, total]，07-D-02） | 08-01 |
| 纯函数 | `clampPreviewTarget(targetMs, totalDurationMs): number`（预览路径 [0, total-1]，Pitfall 2） | 08-01 |
| 纯函数 | `computeKeyboardStepTarget(currentMs, direction, totalDurationMs): number`（±KEYBOARD_STEP_MS，D-12） | 08-01 |
| 常量 | `KEYBOARD_STEP_MS = 5000`、`DensityBucket` 接口（tier: 0\|1\|2\|3） | 08-01 |
| Hook 方法 | `previewSeek(targetMs)` on `usePlaybackEngine`（engine-only，D-01） | 08-01 |
| i18n 键 | `timelineLabel`（en: 'Playback progress' / zh: '播放进度'） | 08-01 |
| 测试文件 | `test/unit/timelineDensity.test.ts` | 08-01 |
| 组件 | `Timeline`（`TimelineProps`：session/status/cues/totalDurationMs/onSeek/onPreviewSeek） | 08-02 |
| CSS 类族 | `.timeline`、`.timeline-time`、`.timeline-track-wrap`、`.timeline-markers`、`.timeline-marker`（.tier-1/2/3）、`.timeline-slider` | 08-02 |
| 集成 | `PlaybackControls` 新增 5 props 并渲染 `<Timeline/>`；`App.tsx` 解构 `seek, previewSeek` 并透传 | 08-02 |

<execution_context>
@/home/haonson/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/haonson/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-timeline-progress-bar/08-CONTEXT.md
@.planning/phases/08-timeline-progress-bar/08-RESEARCH.md
@.planning/phases/08-timeline-progress-bar/08-PATTERNS.md
@.planning/phases/08-timeline-progress-bar/08-VALIDATION.md
@.planning/phases/07-engine-foundation/07-01-SUMMARY.md
@src/playback/session.ts
@src/hooks/usePlaybackEngine.ts
@src/i18n/translations.ts
@test/unit/session.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — 编写 timelineDensity 失败测试（Wave 0）</name>
  <files>test/unit/timelineDensity.test.ts, src/playback/timelineDensity.ts</files>
  <read_first>
    - @.planning/phases/08-timeline-progress-bar/08-VALIDATION.md — Wave 0 要求与 Per-Task Verification Map（用例清单来源）
    - @.planning/phases/08-timeline-progress-bar/08-UI-SPEC.md §密度分桶合同 — 三档阈值锁定：count===0 → tier 0；ratio<=1/3 → tier 1；ratio<=2/3 → tier 2；否则 tier 3（此裁定修正 RESEARCH Pattern 4 草案的 ceil 公式，测试必须按 UI-SPEC 阈值断言）
    - @test/unit/session.test.ts — 测试文件模式：具名 fixture 常量、禁止 Date.now()/fake timers、describe 按被测函数分组、it 描述行为契约
    - @.planning/phases/08-timeline-progress-bar/08-PATTERNS.md §timelineDensity.test.ts — 导入路径（../../src/playback/timelineDensity）与必须覆盖的用例清单
  </read_first>
  <action>
    1. 先创建 `src/playback/timelineDensity.ts` 桩模块，使 RED 因断言失败而非导入失败（TDD gate 要求失败原因正确）：声明 `export interface DensityBucket { startMs: number; endMs: number; tier: 0 | 1 | 2 | 3 }`、`export const KEYBOARD_STEP_MS = 0`（桩值，正确值 5000 在 GREEN 写入）、以及四个函数桩 `computeCueDensityBuckets` → 返回 `[]`、`clampToDuration` → 返回 `0`、`clampPreviewTarget` → 返回 `0`、`computeKeyboardStepTarget` → 返回 `0`。桩文件不含 JSDoc 决策注释（GREEN 阶段补全）。
    2. 创建 `test/unit/timelineDensity.test.ts`（Vitest node 环境，`import { describe, it, expect } from 'vitest'`，从 `../../src/playback/timelineDensity` 导入，`import type { Cue } from '../../src/types/subtitle'`）。顶部具名 fixture 常量（如 `const CUES_SINGLE: Cue[]`），零时钟读取。用例清单：
       - computeCueDensityBuckets：空 cues → `[]`；totalDurationMs <= 0 → `[]`；bucketCount <= 0 → `[]`；返回数组长度 === bucketCount；桶边界 startMs/endMs === i*bucketMs / (i+1)*bucketMs；单 cue 完全落在一个桶 → 该桶 tier 3（ratio 1 > 2/3）其余 tier 0；cue 恰跨桶界（end 在下一桶内部）→ 两个桶都计数；cue end 恰在桶界（如 [0,500]、bucketMs=500）→ 只计入前一桶（半开区间 [start, end)，Pitfall 7）；空桶 tier 0；三档阈值：三个桶 count 为 1/2/3（max=3）→ tier 1/tier 2/tier 3（1/3 落 tier 1、2/3 落 tier 2，UI-SPEC 阈值裁定）；重叠 cue 按数组原序各自计入不重排（07-D-06）→ 同桶计数为 2。
       - clampToDuration：区间内原值返回；负值 → 0；超过 total → total；NaN → 0；Infinity → 0；-Infinity → 0（非有限值一律拒绝为 0，RESEARCH clampToDuration 锁定行为）；totalDurationMs <= 0 → 恒 0。
       - clampPreviewTarget：区间 [0, total-1] 内原值返回；target === total → total-1（预览永不命中精确末端，Pitfall 2）；超过 total → total-1；负值 → 0；NaN → 0；totalDurationMs=0 → 0；totalDurationMs=1 → 0。
       - computeKeyboardStepTarget：direction=1 → current+5000；direction=-1 → current-5000；接近末端 → 夹紧到 total；接近起点 → 夹紧到 0；current 为 NaN → 0；KEYBOARD_STEP_MS === 5000。
    3. 运行测试确认失败原因为断言失败（桩返回退化值），不是模块找不到或语法错误。
  </action>
  <verify>
    <automated>npx vitest run test/unit/timelineDensity.test.ts 2>&1 | grep -E "Tests|failed" | head -5; npx vitest run test/unit/timelineDensity.test.ts > /dev/null 2>&1 && echo "UNEXPECTED-GREEN" || echo "RED-OK"</automated>
  </verify>
  <done>测试文件包含上述全部边界用例；运行结果为 RED-OK（套件失败且失败原因全是断言失败而非导入/语法错误）；桩模块只含签名与退化返回值。提交：test(08-01): add failing tests for timeline density bucketing and clamping</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — 实现 timelineDensity 纯函数模块</name>
  <files>src/playback/timelineDensity.ts</files>
  <read_first>
    - @.planning/phases/08-timeline-progress-bar/08-RESEARCH.md Pattern 4 — computeCueDensityBuckets 实现骨架与 clampToDuration 实现骨架（命名与签名以 RESEARCH 为准；但 tier 映射公式必须按 UI-SPEC 阈值替换草案 ceil 公式）
    - @src/playback/session.ts L1-20 与 L146-173 — 模块头 JSDoc 模式（所属 Phase 与决策编号、不变规则、纯度声明）与边界防御模式（退化输入短路、非有限值拒绝）
    - @.planning/phases/08-timeline-progress-bar/08-PATTERNS.md §timelineDensity.ts — 模块头模仿要点与导入模式
  </read_first>
  <action>
    将桩替换为真实现（签名不变）：
    1. 模块头 JSDoc：声明所属 Phase 8 与决策编号（D-07/D-08/D-09）、不变规则（桶为半开区间 [start, end)；cue 依解析器保序 07-D-06，重叠 cue 按数组顺序各自计入）、纯度声明（reads no clock, never throws）——完全对齐 session.ts 头注释风格。
    2. `computeCueDensityBuckets(cues: Cue[], totalDurationMs: number, bucketCount: number): DensityBucket[]`：退化输入短路（totalDurationMs <= 0 || cues.length === 0 || bucketCount <= 0 → `[]`）；bucketMs = totalDurationMs / bucketCount；对每个 cue 取 first = max(0, floor(cue.start / bucketMs))、last = min(bucketCount - 1, floor((cue.end - 1) / bucketMs))（end-1 保证 end 恰在桶界不溢入下一桶，Pitfall 7），闭区间累加计数；max = 各桶计数最大值；tier 映射按 UI-SPEC 锁定阈值：count === 0 → 0；ratio = count / max，ratio <= 1/3 → 1，ratio <= 2/3 → 2，否则 3（不得使用 RESEARCH 草案的 ceil(count/max*2)——那只有两个非空档位，违反 D-08 三档要求）；返回对象含 startMs = i * bucketMs、endMs = (i + 1) * bucketMs、tier。
    3. `clampToDuration(targetMs: number, totalDurationMs: number): number`：非有限值（含 NaN/±Infinity）→ 0；否则 min(max(targetMs, 0), max(0, totalDurationMs))——提交路径全范围 [0, totalDurationMs]（07-D-02），此处不做减 1 处理（预览钳制是独立函数）。
    4. `clampPreviewTarget(targetMs: number, totalDurationMs: number): number`：先经 clampToDuration 归一，再 min(结果, max(0, totalDurationMs - 1))——预览路径永远停在精确末端之前（Pitfall 2：引擎 tick 在 elapsed >= 末 cue end 时自动停止并清空 session，会导致 Timeline 拖拽中途卸载）。提交路径不得使用本函数（07-D-03 精确末端 = 合法立即结束）。
    5. `KEYBOARD_STEP_MS = 5000`（D-12 步进量）。`computeKeyboardStepTarget(currentMs: number, direction: -1 | 1, totalDurationMs: number): number` = clampToDuration(currentMs + direction * KEYBOARD_STEP_MS, totalDurationMs)。
    6. JSDoc 挂决策编号（07-D-02 / D-12 / Pitfall 2）到各函数。不新增任何其他导出。
  </action>
  <verify>
    <automated>npx vitest run test/unit/timelineDensity.test.ts && npx tsc --noEmit && npm test 2>&1 | tail -3</automated>
  </verify>
  <done>timelineDensity.test.ts 全绿；全套 npm test 无回归（117+ tests）；tsc --noEmit clean；tier 映射为三档确定性阈值（D-08/D-09）。提交：feat(08-01): implement timeline density bucketing and clamping</done>
</task>

<task type="auto">
  <name>Task 3: previewSeek hook 入口 + timelineLabel i18n 键</name>
  <files>src/hooks/usePlaybackEngine.ts, src/i18n/translations.ts</files>
  <read_first>
    - @src/hooks/usePlaybackEngine.ts L114-123（返回类型注解）、L164-181（persist effect 注释——session 对象身份变化驱动 IndexedDB 写入的机制）、L232-239（既有 seek——previewSeek 的镜像模板）、L280（返回对象）— 改此文件必读的四个锚点
    - @.planning/phases/08-timeline-progress-bar/08-RESEARCH.md Pattern 1 — previewSeek 锁定实现（engine-only 镜像）
    - @.planning/phases/08-timeline-progress-bar/08-PATTERNS.md §usePlaybackEngine — useCallback([]) 纪律、返回类型注解与头 JSDoc 同步更新点
    - @src/i18n/translations.ts — en/zh 对称块结构（en 块 L49-55 之后、zh 块 L102-108 之后插入）
  </read_first>
  <action>
    1. `src/hooks/usePlaybackEngine.ts`：在既有 `seek`（L236-239）之后新增 `previewSeek`，与 seek 完全对称但只有一条语句：`engineRef.current?.seek(targetMs)`。刻意的语义：不调度任何 React 状态更新——persist effect 由 session 对象身份变化驱动，引擎位置更新不产生新 session 对象，因此预览路径零 IndexedDB 写入（D-01、07-D-09）。JSDoc 说明：Phase 8 新增、engine-only 拖拽预览入口、playing 态下一帧 tick 捕获新 cue、paused 态引擎 seek 内部立即 findActiveCue + onCueChange、以及预览目标应已由调用侧钳到精确末端之前（Pitfall 2，钳制函数是 Plan 01 的 clampPreviewTarget）。依赖数组 `[]`（hook 内动作回调纪律，无需读任何 prop）。同步三处：返回类型注解（L114-123 区块）加 `previewSeek: (targetMs: number) => void`；hook 头 JSDoc L108 的 Returns 行追加 previewSeek；返回对象 L280 追加 `previewSeek`（置于 seek 之后）。
    2. `src/i18n/translations.ts`：en 块新增 `timelineLabel: 'Playback progress',`，zh 块新增 `timelineLabel: '播放进度',`——两块键序完全对称（既有惯例：每个键在两块中同一位置），可加 `// Timeline.tsx` 区段注释对齐既有风格。不新增其他键（禁用态无可见文案，UI-SPEC Copywriting Contract）。
    3. 绝不触碰：既有 seek、persist effect（L164-181）、play/pause/stop/restoreSession——本任务唯一的行为增量是新增一个只读引擎的入口。
  </action>
  <verify>
    <automated>rg -q "previewSeek" src/hooks/usePlaybackEngine.ts && npx tsc --noEmit && npm test 2>&1 | tail -3 && [ "$(rg -c "timelineLabel" src/i18n/translations.ts)" = "2" ] && [ "$(sed -n '/const previewSeek = useCallback/,/}, \[\])/p' src/hooks/usePlaybackEngine.ts | grep -c 'setSession')" = "0" ] && echo "T3-OK"</automated>
  </verify>
  <done>tsc clean、全套测试绿、en/zh 各含 timelineLabel（rg 计数 === 2）；previewSeek 函数体区域零 session 状态写入（region 检查 === 0，D-01）；previewSeek 出现在返回对象中。提交：feat(08-01): add engine-only previewSeek and timeline label i18n key</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SRT 文件 → 解析器 → cues | 恶意/畸形字幕文件产生极端数值（NaN/Infinity/越界时间）流入密度计算 |
| IndexedDB 持久化记录 → session 恢复 | 注入越界 pausedElapsedMs 的记录可让 Timeline 显示值越界（消费侧在 Plan 08-02 防御，钳制原语在本 plan 交付） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | computeCueDensityBuckets / clampToDuration | medium | mitigate | 退化输入短路（totalDurationMs <= 0 / 空 cues / bucketCount <= 0 → []）；clampToDuration 拒绝一切非有限值为 0；cue start<end 已由解析器验证 |
| T-08-02 | DoS | 密度标记 DOM 节点数 | medium | mitigate | 固定 bucketCount（Plan 08-02 锁定 100）→ 标记 DOM 恒 ≤ bucketCount，与文件大小解耦（5MB 导入上限既有） |
| T-08-03 | Tampering | session 恢复路径的显示值 | low | mitigate | clampToDuration 原语在本 plan 交付；Plan 08-02 在显示侧包裹 sessionElapsedMs 输出（V5 Input Validation，单点钳制） |
| T-08-SC | Tampering | npm 依赖安装 | high | accept | 本 phase 零新依赖（RESEARCH.md Package Legitimacy Audit 空表；v1.2 里程碑锁定），无安装任务，无供应链面 |
</threat_model>

<verification>
- `npx vitest run test/unit/timelineDensity.test.ts` 全绿（密度分桶边界 + 全部钳制函数 + 键盘步进）
- `npm test` 全套无回归（117+ tests）
- `npx tsc --noEmit` clean
- `rg -c "timelineLabel" src/i18n/translations.ts` === 2（en + zh 对称）
- previewSeek 函数体只含引擎调用，无任何 React 状态调度（D-01 自动化 region 检查）
</verification>

<success_criteria>
- TDD 门：RED 提交（test(08-01)）先于 GREEN 提交（feat(08-01)），RED 失败原因为断言而非导入错误
- timelineDensity.ts 五个导出签名与 RESEARCH/UI-SPEC 锁定一致，三档阈值映射为确定性阈值（非 ceil 草案）
- previewSeek 与既有 seek 对称并列于 hook 返回对象，类型注解与头 JSDoc 同步
- 08-VALIDATION.md Wave 0 缺口（timelineDensity.test.ts）补齐
</success_criteria>

<output>
Create `.planning/phases/08-timeline-progress-bar/08-01-SUMMARY.md` when done
</output>
