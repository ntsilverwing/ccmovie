---
phase: 08-timeline-progress-bar
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/Timeline.tsx
  - src/index.css
  - src/components/PlaybackControls.tsx
  - src/App.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "播放页控制区顶部独占一行显示 Timeline：左侧当前时间、右侧总时长，均为 h:mm:ss（UI-01, D-04）"
    - "拖拽中当前时间与字幕画面实时更新且零 IndexedDB 写入（预览走 engine-only 路径，D-01）；松手提交一次并持久化，刷新页面恢复到松手位置（UI-02, D-02）"
    - "播放中拖拽无缝继续播放；暂停中拖拽保持暂停并立即显示目标 Cue 或空白（UI-02, D-03）"
    - "Timeline 显示三档亮度的密度标记（opacity 0.35/0.65/1.0），空档处无任何标记 DOM；标记层垫于已播放段之下（UI-03, D-08, D-09）"
    - "键盘 ←/→ 每次 ±5 秒并夹紧 [0, totalDurationMs]；Home/End 原生行为提交（D-12）"
    - "读屏播报「播放进度, h:mm:ss / h:mm:ss」（aria-label + aria-valuetext，D-13）"
    - "原生 <input type=\"range\"> 是唯一交互与可访问性语义源：键盘/触控/读屏行为由 range 原生提供，密度标记层与轨道视觉为非交互绘制层，不接管 range 的语义和输入（D-10）"
  artifacts:
    - path: "src/components/Timeline.tsx"
      provides: "Timeline 组件：时间标签 + 密度层 + 原生 range 唯一交互源 + 拖拽状态机"
      exports: ["Timeline"]
    - path: "src/index.css"
      provides: ".timeline* 样式族（暗场低亮度、webkit/moz 双伪元素、48px 命中区）"
      contains: ".timeline-slider"
    - path: "src/components/PlaybackControls.tsx"
      provides: "playing/paused 分支首位渲染 <Timeline/>，新增 5 个 props"
      contains: "<Timeline"
    - path: "src/App.tsx"
      provides: "hook 解构补 seek/previewSeek，playback 视图 props 链透传"
      contains: "onPreviewSeek={previewSeek}"
  key_links:
    - from: "src/components/Timeline.tsx"
      to: "src/playback/timelineDensity.ts"
      via: "导入四个纯函数（分桶/钳制/预览钳制/键盘步进），组件壳零业务逻辑"
      pattern: "from '../playback/timelineDensity'"
    - from: "src/App.tsx"
      to: "src/components/PlaybackControls.tsx"
      via: "onSeek={seek} onPreviewSeek={previewSeek} props 透传（App L52 原未解构 seek）"
      pattern: "onPreviewSeek=\\{previewSeek\\}"
    - from: "src/components/PlaybackControls.tsx"
      to: "src/components/Timeline.tsx"
      via: "playing/paused 分支第一个子元素渲染 Timeline"
      pattern: "<Timeline"
    - from: "src/components/Timeline.tsx"
      to: "src/i18n/translations.ts"
      via: "aria-label 使用 t('timelineLabel')（D-13）"
      pattern: "t\\('timelineLabel'\\)"
---

<objective>
交付 Phase 8 的可视层与集成（UI-01/UI-02/UI-03）：`Timeline.tsx` 组件（时间标签 + 密度标记层 + 原生 `<input type="range">` 唯一交互源 + 预览/提交拖拽状态机）、`.timeline*` 暗场低亮度 CSS 族（D-04/D-05/D-06/D-11）、PlaybackControls 集成与 App props 链（补 `seek, previewSeek` 解构），最后以人工验证清单收尾（真机/DevTools 触摸模拟）。

Purpose: Plan 08-01 已交付全部纯函数与 previewSeek 入口；本 plan 把它们接成用户可见可操作的 Timeline——进度显示（UI-01）、拖拽导航（UI-02）、密度指示（UI-03）。视觉与交互契约以已批准的 08-UI-SPEC.md 为准（6/6 checker 通过）。
Output: 可拖拽、可键盘操作、读屏友好、含密度标记的 Timeline 集成进播放控制区；`tsc --noEmit` clean、`npm test` 全绿；人工拖拽验证通过。
</objective>

## Artifacts This Phase Produces

本 phase 创建/修改的全部符号（两 plan 合计清单见 08-01-PLAN.md；本 plan 产出）：`Timeline` 组件与 `TimelineProps` 接口、`.timeline` / `.timeline-time`（-current/-total）/ `.timeline-track-wrap` / `.timeline-markers` / `.timeline-marker`（`.tier-1/2/3`）/ `.timeline-slider` CSS 类族、`PlaybackControls` 新增 props（session/cues/totalDurationMs/onSeek/onPreviewSeek）、`App.tsx` 的 `seek`/`previewSeek` 解构与透传。消费 08-01 产物：`computeCueDensityBuckets`、`clampToDuration`、`clampPreviewTarget`、`computeKeyboardStepTarget`、`KEYBOARD_STEP_MS`、`previewSeek`、`timelineLabel`。

<execution_context>
@/home/haonson/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/haonson/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-timeline-progress-bar/08-CONTEXT.md
@.planning/phases/08-timeline-progress-bar/08-RESEARCH.md
@.planning/phases/08-timeline-progress-bar/08-PATTERNS.md
@.planning/phases/08-timeline-progress-bar/08-UI-SPEC.md
@.planning/phases/08-timeline-progress-bar/08-VALIDATION.md
@src/components/SessionBanner.tsx
@src/components/PlaybackControls.tsx
@src/App.tsx
@src/index.css
@src/hooks/usePlaybackEngine.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Timeline 组件 + .timeline* 暗场 CSS 族</name>
  <files>src/components/Timeline.tsx, src/index.css</files>
  <read_first>
    - @.planning/phases/08-timeline-progress-bar/08-UI-SPEC.md §布局合同 + §密度分桶合同 + §交互合同 + 明确禁止清单 — 已批准的视觉/交互契约，逐条照办（含空桶不渲染 DOM、禁用态、防回跳、兜底提交）
    - @.planning/phases/08-timeline-progress-bar/08-RESEARCH.md Pattern 2 + Pattern 3 — 拖拽状态机（isDragging/dragValue 分流）与键盘步进写法（含为何不能用固定 step 值的 MDN 依据）
    - @src/components/SessionBanner.tsx L30-41 — ticker 模式（本组件 250ms 变体）与导入模式
    - @src/components/PlaybackControls.tsx L77-89 — 代码库唯一 range 先例（aria-label/aria-valuetext 用法）；注意 Timeline 不用 accent-color 快捷样式（需叠加密度层，必须 appearance:none + 伪元素）
    - @.planning/phases/08-timeline-progress-bar/08-PATTERNS.md §Timeline.tsx + §index.css — Props 接口锁定、CSS 色板锚点（L5-14 色板、L244-247 触控惯例、L445-459 landscape 查询位置、L509-515 z-index 惯例）
  </read_first>
  <action>
    创建 `src/components/Timeline.tsx`：
    1. Props 接口照 UI-SPEC 锁定：`session: PlaybackSession | null`、`status: PlaybackStatus`、`cues: Cue[]`、`totalDurationMs: number`、`onSeek: (targetMs: number) => void`、`onPreviewSeek: (targetMs: number) => void`。导入：react 钩子、`sessionElapsedMs`/`formatElapsedHMS` 与 `PlaybackSession` 类型（../playback/session）、`PlaybackStatus` 类型（../hooks/usePlaybackEngine）、`Cue` 类型（../types/subtitle）、`useLanguage`（../i18n/LanguageContext）、08-01 的 `computeCueDensityBuckets`/`clampToDuration`/`clampPreviewTarget`/`computeKeyboardStepTarget`/`KEYBOARD_STEP_MS`（../playback/timelineDensity）。
    2. 模块级常量 `const BUCKET_COUNT = 100`（D-07：一经确定即固定，不随输入规模派生）。组件内 `useMemo(() => computeCueDensityBuckets(cues, totalDurationMs, BUCKET_COUNT), [cues, totalDurationMs])`。
    3. 位置 ticker（SessionBanner 250ms 变体）：`const [now, setNow] = useState(Date.now())`；useEffect 依赖 [status]，仅 status === 'playing' 时 setInterval 250ms 刷新 now，清理 clearInterval。显示值遵守双时钟规则——只读 `sessionElapsedMs(session, now)` wall-clock，绝不读引擎内部时钟（Anti-Pattern 6）。
    4. 拖拽状态机（RESEARCH Pattern 2）：`isDragging` + `dragValue` state；`displayValue = isDragging ? dragValue : clampToDuration(session === null ? 0 : sessionElapsedMs(session, now), totalDurationMs)`（显示侧统一钳制，防注入记录越界——T-08-03 消费点）。禁用判定 `disabled = totalDurationMs <= 0`（Pitfall 6 除零双保险之一）。
    5. onChange：`v = clampToDuration(Number(e.target.value), totalDurationMs)`；isDragging 为真 → `setDragValue(v)` 且 `onPreviewSeek(clampPreviewTarget(v, totalDurationMs))`（预览路径钳到精确末端之前，Pitfall 2）；否则 → `onSeek(v)`（Home/End/PageUp/PageDown 等无 pointerdown 的原生变更直接提交，Pitfall 5）。
    6. pointerdown → setIsDragging(true) + setDragValue(displayValue)；提交函数：若非拖拽态直接返回，否则 setIsDragging(false) + `onSeek(dragValue)`（提交路径不钳末端——命中精确 totalDurationMs = 合法立即结束，07-D-03）。绑定到 onPointerUp、onBlur、onPointerLeave 三处（A2 兜底：个别浏览器不派发 pointerup，UI-SPEC 交互合同锁定）。
    7. onKeyDown：ArrowLeft/ArrowRight → preventDefault 拦截原生步进（step 属性为字符串 any，固定步进值会把拖拽也吸附——MDN 已确认 step 作用于所有输入，Pitfall 3），`onSeek(computeKeyboardStepTarget(displayValue, e.key === 'ArrowLeft' ? -1 : 1, totalDurationMs))`（D-12）。Home/End/PageUp/PageDown 不拦截（原生行为经 onChange 走提交分支）。
    8. 渲染结构：`div.timeline` → `span.timeline-time.timeline-time-current`（formatElapsedHMS(displayValue)）→ `div.timeline-track-wrap` → （非禁用时）`div.timeline-markers`（buckets 中 tier > 0 的桶各渲染一个 `span.timeline-marker`，类名拼接 tier 档位，inline style `left: (b.startMs / totalDurationMs) * 100 + '%'`、`width: (100 / BUCKET_COUNT) + '%'`；tier 0 桶不产生任何 DOM——D-09，因此标记用绝对定位而非 flex 占位）+ `input.timeline-slider`（type=range、min=0、max=totalDurationMs、step 属性为字符串 any、value=displayValue、disabled、上述全部事件、`aria-label={t('timelineLabel')}`、`aria-valuetext` 为 formatElapsedHMS(displayValue) 与 formatElapsedHMS(totalDurationMs) 以 " / " 连接、style 注入 CSS 自定义属性 `--pos-percent`（totalDurationMs > 0 时为百分比字符串，否则 '0%'，守卫除零））→ `span.timeline-time.timeline-time-total`（formatElapsedHMS(totalDurationMs)）。aria 属性中绝不暴露字幕正文（D-13）。
    9. `src/index.css`：在既有 .font-size-slider 规则之后、@media (orientation: landscape) 块（L438）之前插入 .timeline 样式族。要点：`.timeline { flex-basis: 100%; display: flex; align-items: center; gap: 8px; }`（playback-controls 既有 flex-wrap 下独占一行，UI-SPEC 布局合同）；`.timeline-time` 字号 0.85rem / 字重 400 / line-height 1.2 / monospace / min-width 7ch（防抖动），-current 色 #e0e0e0、-total 色 #888（UI-SPEC 色板锁定）；`.timeline-markers` 绝对定位层（高 6px、垂直居中对齐轨道线、pointer-events: none）；`.timeline-marker` 绝对定位 + background #555，.tier-1/2/3 opacity 0.35/0.65/1.0（D-08）；`.timeline-slider` appearance none + background transparent + `touch-action: none`（A1）+ `-webkit-tap-highlight-color: transparent` + `height: 16px; padding: 16px 0; margin: -16px 0`（内容 16 + padding 32 = 48px 命中区，负 margin 抵消零布局位移，D-11/UI-SPEC spacing md+2xl）；webkit 轨道 3px 高 + linear-gradient 双色（#e0e0e0 至 var(--pos-percent)、#333 其后）实现已播放段，moz 用原生 ::-moz-range-track #333 + ::-moz-range-progress #e0e0e0；webkit/moz thumb 14×14 圆形 #e0e0e0、opacity 0 静止隐藏 + 0.15s ease 过渡（D-06），webkit thumb margin-top -5.5px 对齐 3px 轨道；hover/active/focus-visible 以及组件拖拽类（slider 元素 className 在 isDragging 时追加 ` dragging`，CSS 对应 `.timeline-slider.dragging` 两族 thumb opacity 1——触摸拖拽不完全触发 :active 的确定性补充）；`:focus-visible` outline 1px solid #666 offset 4px（UI-SPEC xs token）；`:disabled` 降低不透明度到 0.4。硬性规则：全部颜色为静态字面值，不绑定 var(--subtitle-color)、不随 body.dimmed/high-contrast 变化（UI-SPEC 静态色板规则锁定）；不引入任何新彩色、任何新 z-index 数值（全部位于 .playback-controls z-index 100 层叠上下文内）。
    10. 明确禁止（UI-SPEC Anti-Patterns 1-7）：拖拽预览走 onPreviewSeek 而非提交回调；不把时钟值灌进拖拽中的受控 value；组件内绝无直接持久化调用（写入只经 onSeek 间接触发既有 hook effect）；密度标记绝不绘制在已播放段之上（层叠：markers 在 track 视觉之下）；不为组件测试引入 jsdom。
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -3 && [ "$(rg -c 'step="any"' src/components/Timeline.tsx)" = "1" ] && [ "$(rg -c "aria-valuetext" src/components/Timeline.tsx)" = "1" ] && [ "$(rg -c "t\\('timelineLabel'\\)" src/components/Timeline.tsx)" = "1" ] && rg -q "clampPreviewTarget" src/components/Timeline.tsx && rg -q "computeCueDensityBuckets" src/components/Timeline.tsx && rg -q "computeKeyboardStepTarget" src/components/Timeline.tsx && [ "$(rg -c "saveSession" src/components/Timeline.tsx)" = "0" ] && rg -q "timeline-slider::-webkit-slider-runnable-track" src/index.css && rg -q "timeline-slider::-moz-range-progress" src/index.css && rg -q "\\.timeline-marker\\.tier-1" src/index.css && echo "T1-OK"</automated>
  </verify>
  <done>tsc clean、全套测试绿；range 交互/键盘/aria/预览/提交/兜底全部按 UI-SPEC 交互合同实现；密度层仅渲染 tier>0 桶；样式族覆盖 webkit+moz 双轨、thumb 显隐、48px 命中区、暗场静态色板。提交：feat(08-02): add Timeline component with density markers and dark-theater styling</done>
</task>

<task type="auto">
  <name>Task 2: PlaybackControls 集成 + App props 链</name>
  <files>src/components/PlaybackControls.tsx, src/App.tsx</files>
  <read_first>
    - @src/components/PlaybackControls.tsx L4-21（props 接口模式：onXxx 回调签名、无默认值）、L57-59（playing/paused 分支开头——Timeline 渲染位置）
    - @src/App.tsx L52-56（hook 解构——当前未解构 seek/previewSeek，需补）、L372-389 与 L494-510（两个 PlaybackControls 挂载点）
    - @.planning/phases/08-timeline-progress-bar/08-RESEARCH.md §Props 透传链 — 接口锁定与空安全写法
    - @.planning/phases/08-timeline-progress-bar/08-PATTERNS.md §PlaybackControls.tsx + §App.tsx — 挂载点验证结论（ready 视图实例永远 idle，Timeline 分支永不渲染）
  </read_first>
  <action>
    1. `src/components/PlaybackControls.tsx`：导入 `Timeline`（./Timeline）、`PlaybackSession` 类型（../playback/session）、`Cue` 类型（../types/subtitle）。PlaybackControlsProps 追加五个必填 props：`session: PlaybackSession | null`、`cues: Cue[]`、`totalDurationMs: number`、`onSeek: (targetMs: number) => void`、`onPreviewSeek: (targetMs: number) => void`，并加入解构。在 playing/paused 分支 fragment 的第一个子元素位置（offset −0.5s 按钮之前）渲染 `<Timeline session={session} status={status} cues={cues} totalDurationMs={totalDurationMs} onSeek={onSeek} onPreviewSeek={onPreviewSeek} />`。totalDurationMs <= 0 的禁用分支由 Timeline 自身处理，PlaybackControls 不加额外条件。idle 分支与既有按钮一律不动。
    2. `src/App.tsx`：L52 hook 解构在 stop 之后补 `seek, previewSeek`（对齐 hook 返回对象键序）。两个 PlaybackControls 挂载点都传齐新五 props（props 为必填，TS 要求两处都传；ready 视图实例 status 恒为 idle，Timeline 分支永不渲染，无行为影响）：`session={session}`、`cues={subtitle?.cues ?? []}`、`totalDurationMs={subtitle?.metadata.totalDurationMs ?? 0}`（空安全与 L53 cues 同款）、`onSeek={seek}`、`onPreviewSeek={previewSeek}`。不改动任何既有 props、不触碰 controlsVisible 自动隐藏逻辑（Phase 10 范围）。
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -3 && [ "$(rg -c "<Timeline" src/components/PlaybackControls.tsx)" = "1" ] && [ "$(rg -c "onPreviewSeek" src/components/PlaybackControls.tsx)" -ge 2 ] && [ "$(rg -c "previewSeek" src/App.tsx)" -ge 2 ] && [ "$(rg -c "totalDurationMs" src/App.tsx)" -ge 2 ] && echo "T2-OK"</automated>
  </verify>
  <done>tsc clean、测试绿；Timeline 在 playing/paused 分支首位渲染；App 两个挂载点传齐五 props 且 hook 解构含 seek/previewSeek；props 链 App → PlaybackControls → Timeline 打通（key_links 全部命中）。提交：feat(08-02): integrate Timeline into playback controls with seek props chain</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 人工验证 — 拖拽/键盘/密度/读屏（真机或 DevTools 触摸模拟）</name>
  <files>src/components/Timeline.tsx, src/index.css, src/components/PlaybackControls.tsx, src/App.tsx</files>
  <action>无代码变更——Task 1/2 的自动化工作已完成（组件、样式、集成全部提交，tsc/test 绿）。本任务把构建好的应用交给用户做 08-VALIDATION.md Manual-Only Verifications 清单的验证：node 测试环境无法渲染组件，这些行为（thumb 显隐、拖拽连续性、读屏播报、真机触摸）只能人工判定。执行者先完成 Task 1/2 并跑通自动化验证，然后暂停等待用户按 how-to-verify 清单操作。</action>
  <verify>
    <automated>git status --porcelain | wc -l | grep -q "^0" && echo "CLEAN-TREE-CHECKPOINT"</automated>
  </verify>
  <done>用户逐项核对 how-to-verify 清单（UI-01 布局 / UI-02 拖拽双态与持久化 / D-01 零逐帧写入 / D-03 状态保持 / D-06 手柄显隐 / UI-03 密度三档与空档 / D-12 键盘 ±5s / D-13 读屏 / 触摸连续性），全部通过或问题已记录</done>
  <what-built>播放控制区顶部 Timeline 进度条：左当前时间/右总时长、密度标记层、拖拽预览与提交双路径、键盘 ±5s 步进、原生读屏语义</what-built>
  <how-to-verify>
    1. `npm run dev` 启动，导入含多句字幕的 SRT，进入播放视图并开始播放。
    2. UI-01 布局：控制区顶部独占一行，左为当前时间、右为总时长，格式 h:mm:ss，播放中数字前进、白色填充段随之增长；时间标签无水平抖动。
    3. UI-02 拖拽预览：按住拖动——当前时间与字幕画面实时跟随；松手后刷新页面，恢复到松手位置（证明提交持久化一次）。
    4. D-01 零逐帧写入（选做）：DevTools → Application → IndexedDB，拖动过程中会话记录的 elapsed 不应逐帧变化，松手后才更新一次。
    5. D-03 状态保持：播放中拖拽松手后继续播放不中断；暂停中拖拽松手后仍暂停且画面立即显示目标 Cue 或空白。
    6. D-06 手柄显隐：静止时看不到圆形手柄；悬停/触摸/聚焦后出现。
    7. UI-03 密度：字幕密集段标记更亮（三档亮度）、无字幕空档处轨道干净无标记；已播放白色段覆盖在标记之上。
    8. D-12 键盘：Tab 聚焦 Timeline，按 ←/→ 每次 ±5 秒，到两端被夹紧不越界；End 键跳到末端（播放中提交精确末端会立即结束——07-D-03 期望行为）。
    9. D-13 读屏：DevTools → Accessibility 面板检查该 slider 的 accessible name 为「播放进度」/「Playback progress」，value text 为「h:mm:ss / h:mm:ss」格式。
    10. 触摸（DevTools 设备模拟或真机）：拖动全程平滑不中断、页面不跟随滚动；iOS Safari 真机优先验证（A1/A2）。
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户指针/键盘 → range 输入 → seek 目标 | 用户可控数值输入进入引擎定位路径 |
| 密度数据 → DOM 渲染 | 桶数组驱动 DOM 节点创建 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02 | DoS | .timeline-markers 渲染 | medium | mitigate | 固定 BUCKET_COUNT = 100（模块常量）→ 标记 DOM 恒 ≤ 100 个节点，与 cue 数/文件大小完全解耦 |
| T-08-03 | Tampering | Timeline 显示值 | low | mitigate | 显示侧 clampToDuration 包裹 sessionElapsedMs 输出（Plan 08-01 交付的钳制原语在消费点生效，V5 单点钳制） |
| T-08-04 | Tampering | range 输入值 → seek | low | mitigate | 原生 min/max 属性双保险 + 每个入口（onChange/键盘/pointerdown 初值）经 clampToDuration / clampPreviewTarget / computeKeyboardStepTarget 单点钳制 |
| T-08-SC | Tampering | npm 依赖安装 | high | accept | 本 phase 零新依赖（RESEARCH.md Package Legitimacy Audit 空表），无安装任务，无供应链面 |
</threat_model>

<verification>
- `npx tsc --noEmit` clean；`npm test` 全套绿（含 08-01 的 timelineDensity 套件）
- 交互合同逐条对照 08-UI-SPEC §交互合同：预览钳末端之前、提交不钳、兜底提交三处绑定、键盘拦截 ±5s、防回跳分流、禁用态、250ms ticker
- UI-SPEC 色板/排版/间距/层级合同在 CSS 中逐条落位（静态色板、0.85rem/400/monospace/7ch、48px 命中、markers 在已播放段之下）
- ROADMAP 成功标准人工验证：进度条时间显示（标准 1）、双态拖拽无缝（标准 2）、密度标记（标准 3）——Task 3 清单覆盖
</verification>

<success_criteria>
- Timeline 组件为 UI-SPEC 布局/色板/文案合同的忠实实现，无偏离锁定值
- props 链 App → PlaybackControls → Timeline 完整（seek + previewSeek 双路径均可达）
- 人工验证清单 10 项全部通过或明确记录问题
- 零新依赖（package.json / package-lock.json 无变更）
</success_criteria>

<output>
Create `.planning/phases/08-timeline-progress-bar/08-02-SUMMARY.md` when done
</output>
