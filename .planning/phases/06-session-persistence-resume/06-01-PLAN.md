---
phase: 06-session-persistence-resume
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/playback/session.ts
  - src/db/database.ts
  - src/db/sessions.ts
  - test/unit/session.test.ts
  - test/unit/sessions.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - FILE-03
must_haves:
  truths:
    - "isSessionExpired(session, now, expiryMs): age == expiryMs is NOT expired (session kept); age == expiryMs + 1ms is expired (record cleared on load). Negative now - startedAt clock skew clamps the elapsed display to 0:00:00."
    - "An existing v1 install opening the v2 build keeps every record in the subtitles store and gains a new session object store, because the upgrade callback is guarded by an oldVersion < 1 / oldVersion < 2 ladder."
    - "The session store holds exactly one record under the fixed key 'current'; a second save under that key overwrites the previous record (replacement semantics), so only one session record ever exists."
    - "loadSession returns null for any structurally invalid or corrupt record and never throws; saveSession and clearSessionRecord swallow errors to console.warn so persistence failure never crashes playback."
    - "session.ts stays pure and clock-free: every wall-clock value enters via an explicit now parameter and all functions are total (never throw), matching the module's existing purity contract."
  artifacts:
    - "src/playback/session.ts exports SESSION_EXPIRY_MS (= 21600000, i.e. 6 * 3_600_000), isSessionExpired(session, now, expiryMs), and isValidSession(raw)"
    - "src/db/database.ts opens cinemasyncsubs at version 2 with the guarded upgrade ladder and a CinemaSyncDB.session store entry (string key, PlaybackSession value)"
    - "src/db/sessions.ts exports SESSION_KEY = 'current', loadSession(), saveSession(session), clearSessionRecord()"
    - "test/unit/session.test.ts extended with expiry boundary and validation-reject cases, all green"
    - "test/unit/sessions.test.ts created with fake-indexeddb upgrade/CRUD cases, all green"
    - "package.json lists fake-indexeddb as a devDependency"
  key_links:
    - "src/db/sessions.ts imports getDB from ./database and isValidSession from ../playback/session — loadSession validates every record read and clears corrupt records best-effort (empty store stays untouched)"
    - "The v1->v2 upgrade path is exercised by a fake-indexeddb test that seeds a v1 database and reopens it at v2"
---

<objective>
Build the persistence core for FILE-03: (1) pure, clock-injected session
expiry and shape-validation policy added to the existing session module, and
(2) the IndexedDB session store — a version-2 schema upgrade with an
oldVersion-guarded ladder plus a single-record ('current') CRUD module that
never throws into the playback path.

Purpose: every later plan (hook resume path, resume card) depends on these
primitives existing and being provably correct at the boundaries.
Output: tested session.ts predicates, v2 database schema, sessions.ts store
module, two green test suites.
</objective>

<execution_context>
@/home/haonson/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/haonson/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-persistence-resume/06-RESEARCH.md
@.planning/phases/06-session-persistence-resume/06-PATTERNS.md
@src/playback/session.ts
@src/db/database.ts
@src/db/subtitles.ts
@test/unit/session.test.ts
</context>

## Artifacts this phase produces

- `src/playback/session.ts` — adds exports `SESSION_EXPIRY_MS`, `isSessionExpired(session, now, expiryMs)`, `isValidSession(raw)`
- `src/db/database.ts` — `CinemaSyncDB` gains the `session` object store; `openDB` bumped to version 2 with an `oldVersion < 1` / `oldVersion < 2` upgrade ladder
- `src/db/sessions.ts` (new) — `SESSION_KEY = 'current'`; `loadSession`, `saveSession`, `clearSessionRecord`
- `src/hooks/usePlaybackEngine.ts` — adds `restoreSession(cues, persisted)` and a guarded write-on-change persist effect (plan 06-02)
- `src/components/ResumeCard.tsx` (new) — `ResumeCard` component (plan 06-03)
- `src/App.tsx` — `persistedSession` state, boot session-load effect, resume/dismiss handlers, conditional `ResumeCard` mounts (plan 06-03)
- `test/unit/session.test.ts` — extended (expiry boundaries, validation reject matrix)
- `test/unit/sessions.test.ts` (new) — fake-indexeddb CRUD + v1→v2 upgrade tests
- `test/unit/playbackEngine.test.ts` — extended (restore ordering contract, plan 06-02)
- `package.json` / `package-lock.json` — `fake-indexeddb` devDependency

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — session expiry + validation predicates in session.ts</name>
  <files>src/playback/session.ts, test/unit/session.test.ts</files>
  <read_first>
    - src/playback/session.ts (the file being modified — obey its header purity contract: no clock reads, all functions pure and total)
    - test/unit/session.test.ts (injected-fixture convention: const T = 1_000_000; never Date.now(), never fake timers)
    - .planning/phases/06-session-persistence-resume/06-PATTERNS.md (section "src/playback/session.ts (edit — add isSessionExpired, isValidSession)")
    - .planning/phases/06-session-persistence-resume/06-RESEARCH.md (Pattern 5 expiry design; "Policy module shape" code example; Pitfall 8-style validation spirit = reject, never clamp)
  </read_first>
  <behavior>
    - Test 1: isSessionExpired returns false when (now - session.startedAt) == expiryMs exactly (strictly-greater contract: exactly-at-threshold is still valid)
    - Test 2: isSessionExpired returns true when (now - session.startedAt) == expiryMs + 1
    - Test 3: isSessionExpired returns false when now - startedAt is negative (clock skew: age clamped by sign, never expired)
    - Test 4: isValidSession accepts a well-formed PlaybackSession (pausedElapsedMs null AND a numeric pausedElapsedMs variant)
    - Test 5 (reject matrix, it.each): missing subtitleId; non-string fileName; non-finite startedAt (NaN/Infinity); non-number offsetMs; pausedElapsedMs of wrong type (e.g. a string); non-object input (null, undefined, a string) — each returns false, never throws
  </behavior>
  <action>
    RED: Extend test/unit/session.test.ts following its injected-T convention (const T = 1_000_000; BASE = { subtitleId: 'sub-42', fileName: 'movie.srt', offsetMs: 0 }). Add describe blocks for isSessionExpired (the three boundary cases above, passing expiryMs explicitly) and isValidSession (accept cases + it.each reject matrix). Run: npx vitest run test/unit/session.test.ts — the new cases MUST fail (exports do not exist yet). Commit as the RED step per house convention.
    GREEN: In src/playback/session.ts, add three exports and nothing else. (1) export const SESSION_EXPIRY_MS = 6 * 3_600_000 — planner-locked value per RESEARCH Open Question 1 recommendation (flagged assumption PA-1: threshold 6h, measured from startedAt per 06-PATTERNS.md; strictly-greater comparison so exactly-at-threshold is valid). (2) export function isSessionExpired(session: PlaybackSession, now: number, expiryMs: number): boolean returning (now - session.startedAt) > expiryMs. (3) export function isValidSession(raw: unknown): raw is PlaybackSession doing per-field checks in the loadSettings spirit (typeof string for subtitleId/fileName, Number.isFinite for startedAt/offsetMs, pausedElapsedMs === null || Number.isFinite(pausedElapsedMs)) and returning a boolean without ever throwing. Honor the module header: no clock reads, pure, total, fresh objects only — no mutation, no new imports, no idb/DOM references. Keep JSDoc density matching the existing functions. Re-run the suite until green; then run the full suite (npm test) to confirm no regressions.
  </action>
  <verify>
    <automated>npx vitest run test/unit/session.test.ts &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export const SESSION_EXPIRY_MS" src/playback/session.ts succeeds
    - grep -q "export function isSessionExpired" src/playback/session.ts succeeds
    - grep -q "export function isValidSession" src/playback/session.ts succeeds
    - npx vitest run test/unit/session.test.ts exits 0 and includes passing cases for the expiry == threshold (valid), threshold+1 (expired), and negative-age boundary
    - npm test exits 0 (all 7 pre-existing suites still green)
    - Comment-stripped clock gate: grep -vE '^\s*(\*|//|/\*)' src/playback/session.ts | grep -c -E 'Date\.|performance\.' returns 0 — the mandated module JSDoc legitimately NAMES Date.now()/performance.now() (dual-clock split rationale), so comment lines (/** openers, * interiors, // line comments) are excluded; zero clock reads outside comments (05-01-proven gate)
  </acceptance_criteria>
  <done>All new predicate tests pass; module purity preserved; exports present with exact signatures isSessionExpired(session, now, expiryMs) and isValidSession(raw).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave-0 install + TDD — IndexedDB session store (v2 schema) and sessions.ts CRUD</name>
  <files>src/db/database.ts, src/db/sessions.ts, test/unit/sessions.test.ts, package.json, package-lock.json</files>
  <read_first>
    - src/db/database.ts (the file being modified — current v1 upgrade body creates subtitles unconditionally; dbPromise is a memoized singleton)
    - src/db/subtitles.ts (style analog: async function, try/catch, singleton getDB, JSDoc on every export)
    - .planning/phases/06-session-persistence-resume/06-PATTERNS.md (sections "src/db/database.ts (edit)" and "src/db/sessions.ts (new)" — includes the exact replacement pattern and the swallow-warn deviation)
    - .planning/phases/06-session-persistence-resume/06-RESEARCH.md (Pattern 1 guarded upgrade, Pitfall 1 ConstraintError, fake-indexeddb usage example — memoized dbPromise requires module-graph isolation per test)
    - src/playback/session.ts (PlaybackSession interface — the record shape stored verbatim)
  </read_first>
  <behavior>
    - Test 1 (upgrade preservation): seed a v1 database (openDB 1 with the original unconditional upgrade creating subtitles + indexes, insert one subtitle row, close), reopen through the app's getDB() at v2 — the subtitle row survives and the 'session' object store exists
    - Test 2 (round-trip): saveSession persists a full PlaybackSession under key 'current'; loadSession returns every field verbatim (subtitleId, fileName, startedAt, offsetMs, pausedElapsedMs null and numeric variants)
    - Test 3 (replacement): saveSession(A) then saveSession(B with different offsetMs) under the same key — loadSession returns B only
    - Test 4 (dismiss-clear): saveSession then clearSessionRecord — loadSession returns null
    - Test 5 (corrupt reject + best-effort clear): a hand-inserted malformed record (wrong field types) makes loadSession return null without throwing; after the fired best-effort clear settles (flush a macrotask), the record under 'current' is gone — a raw db.get returns undefined (RESEARCH Pattern 5 clear-on-invalid; the wiring behind 06-03 truth 3, T-06-01-01, and checkpoint step 6's corrupt subcase)
    - Test 6 (empty-store invariant): with nothing under 'current', loadSession returns null and issues NO write or delete — vi.spyOn(IDBObjectStore.prototype, 'delete') is never called (a delete on a missing key would be a silent no-op, so a post-state diff cannot observe it); the initial-mount empty state must stay mutation-free (06-03 truth 2)
  </behavior>
  <action>
    WAVE 0 INSTALL: run npm install --save-dev fake-indexeddb@^6.2.5 (legitimacy verdict OK per RESEARCH Package Legitimacy Audit — 6.2.5, 5.0M downloads/week, no postinstall). Confirm package.json devDependencies.
    RED: Create test/unit/sessions.test.ts. First line: import 'fake-indexeddb/auto'. Import { IDBFactory, IDBObjectStore } from 'fake-indexeddb' and reset per test in beforeEach via indexedDB = new IDBFactory(); because src/db/database.ts memoizes dbPromise, call vi.resetModules() and dynamically re-import the database/sessions modules inside each test (RESEARCH code-example isolation guidance). Write the six behavior cases above, seeding v1 via a local openDB call that replicates the original v1 upgrade body before reopening through the app's getDB(). For Test 5, flush the fire-and-forget clear after loadSession resolves (await a macrotask) before asserting the raw re-get is undefined; for Test 6, vi.spyOn(IDBObjectStore.prototype, 'delete') before the call and assert it was never called (restore spies per the file's afterEach pattern). Run: npx vitest run test/unit/sessions.test.ts — MUST fail (modules/stores do not exist yet). Commit the RED step.
    GREEN: Edit src/db/database.ts: extend CinemaSyncDB with session: { key: string; value: PlaybackSession } (import type PlaybackSession from '../playback/session'); bump openDB('cinemasyncsubs', 2, ...) and restructure the upgrade body as an oldVersion ladder — if (oldVersion < 1) wraps the existing subtitles create + both indexes; if (oldVersion < 2) creates db.createObjectStore('session') with out-of-line keys. Keep the dbPromise memoization untouched. Create src/db/sessions.ts mirroring subtitles.ts structure/JSDoc density but with the swallow-warn, never-throw policy (persistence failure must never crash playback, 06-PATTERNS.md deviation note): const SESSION_KEY = 'current' as const; async loadSession(): Promise&lt;PlaybackSession | null&gt; = get via db.get('session', SESSION_KEY); if raw === undefined (empty store) return null with NO further action — an empty store must never trigger a write or delete; if raw exists but isValidSession(raw) fails, fire void clearSessionRecord() WITHOUT awaiting (best-effort clear-on-invalid per RESEARCH Pattern 5; clearSessionRecord already swallow-warns internally, so the fired clear can never reject into the playback path) and return null; otherwise return raw; catch → console.warn('Failed to load session:', err) and return null; async saveSession(session: PlaybackSession): Promise&lt;void&gt; = db.put('session', session, SESSION_KEY), catch → console.warn('Failed to save session:', err); async clearSessionRecord(): Promise&lt;void&gt; = db.delete('session', SESSION_KEY), catch → console.warn('Failed to clear session:', err). Run the suite green, then npm test and npm run build (tsc -b) for full-suite + typecheck gates.
  </action>
  <verify>
    <automated>npx vitest run test/unit/sessions.test.ts &amp;&amp; npm test &amp;&amp; npm run build</automated>
  </verify>
  <!-- planner-discipline-allow: throw -->
  <acceptance_criteria>
    - npm ls fake-indexeddb shows 6.x installed as devDependency
    - grep -q "oldVersion < 1" src/db/database.ts and grep -q "oldVersion < 2" src/db/database.ts both succeed
    - grep -q "openDB<CinemaSyncDB>('cinemasyncsubs', 2" src/db/database.ts succeeds
    - grep -q "createObjectStore('session')" src/db/database.ts succeeds
    - grep -q "export async function loadSession" src/db/sessions.ts, grep -q "export async function saveSession" src/db/sessions.ts, grep -q "export async function clearSessionRecord" src/db/sessions.ts all succeed
    - grep -q "SESSION_KEY = 'current'" src/db/sessions.ts succeeds
    - src/db/sessions.ts source contains no exception-raising path: grep -v "^\s*\*" src/db/sessions.ts | grep -v "^\s*//" | grep -v "^\s*/\*" | grep -c "throw" equals 0 (swallow-warn policy)
    - npx vitest run test/unit/sessions.test.ts exits 0, including the v1-to-v2 upgrade-preservation case, the seeded-invalid-record-cleared-after-loadSession case, and the empty-store no-delete case
    - In loadSession, the empty-store (undefined raw) branch returns before any delete call, and the invalid-record branch fires clearSessionRecord without awaiting it (verifiable by line order in src/db/sessions.ts)
    - npm test exits 0 and npm run build exits 0
  </acceptance_criteria>
  <done>v2 schema upgrade preserves existing subtitle rows; session-store CRUD round-trips a full PlaybackSession under key 'current'; invalid records read as null and are cleared best-effort (empty store untouched); nothing in sessions.ts can throw into the playback path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB → app | Persisted session record read at boot is untrusted input (user/devtools/extension writable) |
| npm registry → devDependencies | fake-indexeddb install is a supply-chain crossing |
| Device clock → session math | startedAt/expiry computed against a user-controllable wall clock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01-01 | Tampering | loadSession / boot hydration | high | mitigate | isValidSession shape-validates every field on read; invalid or corrupt records are discarded as absent (and cleared); the module is total and never throws into the playback path |
| T-06-01-02 | Tampering (clock skew) | isSessionExpired / elapsed math | medium | mitigate | Strictly-greater expiry predicate bounds staleness; negative age (now < startedAt) is treated as not-expired and the shared formatElapsedHMS clamps display to 0:00:00 — verified by boundary tests |
| T-06-01-03 | Denial of Service (self) | getDB upgrade callback | high | mitigate | oldVersion-guarded ladder prevents ConstraintError that would reject the dbPromise singleton and break both stores; v1→v2 reopen path covered by a fake-indexeddb test |
| T-06-01-SC | Tampering (supply chain) | npm install fake-indexeddb | high | mitigate | Package Legitimacy Audit verdict OK (6.2.5, 5.0M dl/wk, no postinstall); slopcheck clean; devDependency only, never shipped to the client bundle |
</threat_model>

<verification>
- npx vitest run test/unit/session.test.ts test/unit/sessions.test.ts — both suites green
- npm test — full suite (now 8 files: 7 pre-existing + sessions.test.ts) green
- npm run build — tsc -b clean
- Manual spot-check not required for this plan (pure + store logic fully automated)
</verification>

<success_criteria>
- SESSION_EXPIRY_MS exported as 6 * 3_600_000; expiry strictly-greater on startedAt with exactly-at-threshold valid
- isValidSession rejects malformed records across the full reject matrix without throwing
- cinemasyncsubs opens at version 2; the guarded ladder preserves v1 data and adds the 'session' store
- sessions.ts CRUD round-trips under fake-indexeddb with swallow-warn error policy
</success_criteria>

<output>
Create `.planning/phases/06-session-persistence-resume/06-01-SUMMARY.md` when done
</output>
