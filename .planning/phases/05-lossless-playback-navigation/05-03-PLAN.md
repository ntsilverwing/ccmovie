---
phase: 05-lossless-playback-navigation
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified: [src/i18n/translations.ts, src/components/SessionToast.tsx, src/components/SessionBanner.tsx, src/components/PlaybackControls.tsx, src/index.css]
autonomous: true
requirements: [PLAY-08]
must_haves:
  truths:
    - "D-06: SessionBanner persists for the session's entire life atop the selection page — movie title, live elapsed readout, and one-tap resume CTA — mounted above the .saved-movies region per the CONTEXT mount-point contract"
    - "D-07 (Phase-6 continuity lock): .session-banner class base, DOM order [text block][resume][dismiss], and mount point are FINAL — Phase 6's resume card layers IndexedDB provenance onto this identical component without relocation or restyle"
    - "D-08: the dismiss × is a single-tap button carrying aria-label dismissSession (放弃本次播放 / Abandon session) — the stop-and-abandon wiring lands in Plan 05-04, but no confirmation dialog exists at any layer"
    - "D-09: the back control is a single tap with no confirm/double-tap/long-press — mis-tap defense is purely low visual weight (transparent fill, no border, color #888, opacity 0.55, brightening to #e0e0e0 at opacity 1 on :active) plus full reversibility via the banner resume"
    - "UI-considerations [empty]: with no active session, banner & toast render nothing — the selection page is byte-identical to v1.0 (both components return null pre-session / pre-first-trigger)"
    - "UI-considerations [populated]: a playing session shows title #e0e0e0 weight 500 above a 已播 h:mm:ss monospace meta in #888 refreshed on a 1000ms interval tick (I-7)"
    - "UI-considerations [partial/paused]: a paused session switches the meta line to 已暂停 h:mm:ss with the value frozen at pause time (frozen via session.pausedElapsedMs); resume CTA unchanged"
    - "UI-considerations [loading]: session state is synchronous in-memory — no async/loading surface exists in Phase 5; IndexedDB arrival defers to Phase 6"
    - "UI-considerations [overflow]: unbounded hours (24:00:00+) flow in a flex:1 + min-width:0 + monospace meta block — meta line is never truncated"
    - "UI-considerations [zero-one-many]: session cardinality is 0‖1 by design (v1 single-session model) — no plural copy exists anywhere in the new keys"
    - "UI-considerations [long-text]: movie title is single-line nowrap + ellipsis (mirrors .saved-movie-name); the toast wraps at most 2 lines, centered, within max-width 90vw"
    - "Canon referral (Phase-1, breadcrumb only): banner and toast render fileName and copy exclusively as React text nodes — the existing subtitle-XSS canon (never dangerouslySetInnerHTML) is inherited, no new HTML sink is introduced"
    - "i18n is type-locked bilingual: every new key exists in BOTH en and zh or tsc fails; zh for sessionKeptToast is pinned to the REQUIREMENTS PLAY-08 wording 原字幕已保留，可直接继续播放"
  artifacts:
    - path: src/components/SessionBanner.tsx
      provides: "Session banner — title + elapsed/paused meta + resume + dismiss, null-render without session"
      exports: [SessionBanner]
    - path: src/components/SessionToast.tsx
      provides: "Passive kept-session toast — role=status, 3500ms auto-fade, pointer-events none"
      exports: [SessionToast]
    - path: src/components/PlaybackControls.tsx
      provides: "edited — optional onBack prop + .playback-back first-child of the playing‖paused branch"
      contains: "playback-back"
    - path: src/i18n/translations.ts
      provides: "six new session keys in both en and zh under a single component-comment section"
      contains: "sessionKeptToast"
    - path: src/index.css
      provides: ".session-banner family, .session-toast, .playback-back — all colors ≤ #222 fills per cinema luminance rule"
      contains: ".session-banner"
  key_links:
    - from: src/components/SessionBanner.tsx
      to: src/playback/session.ts
      via: "imports sessionElapsedMs + formatElapsedHMS for the ticking meta line"
      pattern: "from '../playback/session'"
    - from: src/components/SessionBanner.tsx
      to: src/i18n/translations.ts
      via: "t() keys resumePlaying / resumePlayingAria / sessionElapsed / sessionPausedAt / dismissSession"
      pattern: "t\\('session(Elapsed|PausedAt)'"
    - from: src/components/PlaybackControls.tsx
      to: src/i18n/translations.ts
      via: "back control reuses the existing back key with ‹ prefix (UI-SPEC copywriting contract)"
      pattern: "‹ {t\\('back'\\)}"
  prohibitions:
    - { statement: "No confirmation dialogs, double-tap gates, or long-press guards on any Phase-5 surface (back control, resume CTA, dismiss ×) — reversibility is the only mis-tap defense; added theater-dark interaction friction is explicitly vetoed by CONTEXT specifics", status: unverified, flagged: true }
---

<objective>
Build the three UI surfaces of PLAY-08 — the persistent session banner (D-06), the one-shot kept-session toast, and the low-visual-weight back control (D-09) — plus their bilingual copy and cinema-dark CSS, exactly per the approved UI-SPEC. Pure presentation: every behavior prop (onResume/onDismiss/onBack) and the session data arrive from Plan 05-04's wiring; session math comes from Plan 05-01's tested module.

Purpose: Splitting surfaces from integration keeps each plan inside its context budget and lets 05-04 wire against finished components.
Output: 2 new components, 1 edited component, 6 new i18n keys ×2 languages, 3 CSS class families.
</objective>

<execution_context>
@/home/haonson/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/haonson/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-lossless-playback-navigation/05-CONTEXT.md
@.planning/phases/05-lossless-playback-navigation/05-UI-SPEC.md
@.planning/phases/05-lossless-playback-navigation/05-PATTERNS.md
@src/i18n/translations.ts
@src/i18n/LanguageContext.tsx
@src/components/PlaybackControls.tsx
@src/components/CuePreview.tsx
@src/pwa/RotateOverlay.tsx
@src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n session keys + SessionToast component + toast CSS</name>
  <files>src/i18n/translations.ts, src/components/SessionToast.tsx, src/index.css</files>
  <read_first>
    - src/i18n/translations.ts (whole file — append layout, per-component comment grouping, as-const tail)
    - src/pwa/RotateOverlay.tsx (closest overlay-component skeleton: hook ordering, conditional-null render, doc-comment style)
    - src/i18n/LanguageContext.tsx (t() signature; key-parity mechanism: translations[lang][key] indexing makes missing zh keys a tsc error)
  </read_first>
  <action>
    1) In src/i18n/translations.ts append a new comment section `// SessionBanner.tsx / SessionToast.tsx` inside BOTH the en and zh objects (same position, same key order in each): resumePlaying ('Resume' / '继续播放'), resumePlayingAria ('Resume {fileName}' / '继续播放《{fileName}》'), sessionKeptToast ('Subtitle kept — resume anytime' / '原字幕已保留，可直接继续播放' — zh string is locked by REQUIREMENTS PLAY-08), sessionElapsed ('{time} elapsed' / '已播 {time}'), sessionPausedAt ('Paused at {time}' / '已暂停 {time}'), dismissSession ('Abandon session' / '放弃本次播放'). Planner note (deviation, documented): UI-SPEC's copywriting table lists the resume aria-label under the resumePlaying row with a {fileName} interpolation, but one key cannot hold both `Resume` and `Resume {fileName}` — resolved as a separate resumePlayingAria key; both are present in en and zh.
    2) Create src/components/SessionToast.tsx following RotateOverlay's skeleton: props { trigger: number | null }; local visible state (init false); a useEffect keyed on [trigger] that, when trigger is non-null, sets visible true, arms a 3500ms timeout setting visible false, and returns a cleanup clearing that timeout; render null until the first trigger arrives; root div className `session-toast${visible ? '' : ' hidden'}`, role="status", text content t('sessionKeptToast'). No pointer interaction anywhere (I-4 passive).
    3) In src/index.css append a .session-toast block: position fixed, top 12px, left 50% with translateX(-50%), max-width 90vw, z-index 300, padding 12px 20px, background rgba(17,17,17,0.95), border 1px solid #333, border-radius 8px, color #e0e0e0, font-size 1rem, font-weight 400, line-height 1.5, text-align center, pointer-events none, opacity 1, transition opacity 0.3s ease (mirroring the .playback-controls fade idiom); sibling rule .session-toast.hidden { opacity: 0 }. Keep border 1px solid #333 on the toast as the sole luminance boundary.
  </action>
  <verify>
    <automated>npm run build && test "$(grep -c "sessionKeptToast: '原字幕已保留，可直接继续播放'" src/i18n/translations.ts)" = "1" && test "$(grep -c 'role="status"' src/components/SessionToast.tsx)" = "1" && test "$(grep -c '3500' src/components/SessionToast.tsx)" = "1"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 — tsc structurally proves en/zh key parity for all six new keys
    - translations.ts contains the exact locked zh string above exactly once, and identical key order in en and zh sections
    - SessionToast.tsx: role="status" appears once; 3500 timeout literal appears once; renders null before first trigger; no onClick/onPointer handler exists in the file
    - index.css gains .session-toast and .session-toast.hidden rules with the values listed in the action
  </acceptance_criteria>
  <done>Six bilingual keys live and type-checked; SessionToast fades after 3500ms with a 0.3s opacity transition; toast styles match the UI-SPEC color/spacing tables</done>
</task>

<task type="auto">
  <name>Task 2: SessionBanner component + banner CSS</name>
  <files>src/components/SessionBanner.tsx, src/index.css</files>
  <read_first>
    - src/playback/session.ts (05-01 exports: PlaybackSession, sessionElapsedMs, formatElapsedHMS)
    - src/hooks/usePlaybackEngine.ts (PlaybackStatus type to import for the status prop)
    - src/App.tsx lines 197-214 (saved-movie-item DOM order analog: [flex:1 button][× delete])
    - src/components/CuePreview.tsx line 103 (t() param interpolation idiom)
    - src/pwa/RotateOverlay.tsx (component skeleton / hook ordering / null-render)
    - .planning/phases/05-lossless-playback-navigation/05-UI-SPEC.md Component Inventory + Color + Typography + Spacing tables (all class values)
  </read_first>
  <action>
    Create src/components/SessionBanner.tsx: props { session: PlaybackSession | null; status: PlaybackStatus; onResume: () => void; onDismiss: () => void }. Return null when session is null or status is 'idle' (defensive — 05-04 clears session on idle anyway). Body className session-banner with EXACT final DOM order (D-07 lock): first child div.session-banner-text (flex:1, min-width:0) containing div.session-banner-title (session.fileName, plain text node) and div.session-banner-meta; second child button.session-banner-resume type="button" onClick=onResume, label t('resumePlaying'), aria-label t('resumePlayingAria', { fileName: session.fileName }); third child button.session-banner-dismiss type="button" onClick=onDismiss, aria-label t('dismissSession'), text content '×' (PATTERNS saved-movie-delete glyph precedent). Meta line: status === 'paused' → t('sessionPausedAt', { time }) else t('sessionElapsed', { time }), where time = formatElapsedHMS(sessionElapsedMs(session, now)). Ticking (I-7): local now state initialized Date.now(); a useEffect that arms a 1000ms setInterval calling setNow(Date.now()) ONLY while status === 'playing', with cleanup clearing the interval (interval-in-effect with cleanup return — PATTERNS "No Analog Found" row 3 guidance). Title uses the .saved-movie-name whitespace treatment; meta explicitly must NOT be truncated.
    In src/index.css append the .session-banner family per the UI-SPEC tables: .session-banner { display flex, align-items center, gap 8px, padding 12px 16px, margin-bottom 16px, background #111, border 1px solid #333, border-radius 6px } / .session-banner-text { flex 1, min-width 0 } / .session-banner-title { font-size 1rem, font-weight 500, color #e0e0e0, white-space nowrap, overflow hidden, text-overflow ellipsis, max-width 100% } / .session-banner-meta { font-size 0.75rem, font-family monospace, color #888, margin-top 4px — the 4px meta gap override replacing the legacy 2px from .saved-movie-meta } / .session-banner-resume { flex-shrink 0, height 48px, padding 0 16px, font-size 1rem, font-weight 500, color #fff, background #222, border 1px solid #666, border-radius 6px, -webkit-tap-highlight-color transparent } — this accent treatment is the phase's single highest-luminance interactive, per the UI-SPEC color table / .session-banner-dismiss { flex-shrink 0, width 48px, height 48px, background transparent, border none, color #888, font-size 1.2rem, -webkit-tap-highlight-color transparent } / .session-banner-dismiss:active { background #331111, color #fff } — mirrors the existing .saved-movie-delete:active destructive-idle pattern exactly.
  </action>
  <verify>
    <automated>npm run build && test "$(grep -c 'session-banner' src/components/SessionBanner.tsx)" -ge "5" && test "$(grep -c 'session-banner-dismiss:active' src/index.css)" = "1" && test "$(grep -c '1000' src/components/SessionBanner.tsx)" -ge "1"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 (session.ts imports + props typing are sound)
    - DOM order in JSX is exactly text-block → resume → dismiss (D-07 final)
    - setInterval appears exactly once, armed only in the playing branch, cleared in the effect cleanup
    - aria-labels wired: resume → resumePlayingAria with fileName param; dismiss → dismissSession
    - All CSS values above present in index.css; no rule anywhere in this plan's CSS uses a fill brighter than #222 (cinema luminance rule)
    - grep -c 'dangerouslySetInnerHTML' src/components/SessionBanner.tsx returns 0 (text-node rendering only)
  </acceptance_criteria>
  <done>Banner renders the D-06 trio (title / elapsed-or-paused meta / resume) plus dismiss, ticks once per second while playing, freezes while paused, null-renders without a session, and carries the Phase-6-locked class base + DOM order</done>
</task>

<task type="auto">
  <name>Task 3: PlaybackControls low-visual-weight back control + CSS</name>
  <files>src/components/PlaybackControls.tsx, src/index.css</files>
  <read_first>
    - src/components/PlaybackControls.tsx (props interface lines 4-21; playing‖paused branch around line 47-58 — insertion point before the offset −0.5s button)
    - src/App.tsx lines 226-232 (existing back-button label construction — copy the label shape, not the positioning)
    - .planning/phases/05-lossless-playback-navigation/05-UI-SPEC.md Color table's "Back control low-visual-weight treatment (D-09)" block + Spacing table's .playback-back row
  </read_first>
  <action>
    In src/components/PlaybackControls.tsx add optional prop onBack?: () => void to PlaybackControlsProps (callbacks-first ordering preserved). Inside the playing‖paused fragment, guarded by onBack being defined, render as the FIRST child (before the offset minus-button) a button.playback-back, type="button", onClick=onBack, aria-label t('back'), whose visible label is the existing back key prefixed with the ‹ glyph — identical label construction to the App.tsx ready-view back button. It deliberately reuses the family font size but NOT .control-button's background or border (UI-SPEC typography rule). Single tap, no other gesture handling (D-09).
    In src/index.css append .playback-back { min-width 48px, min-height 48px, padding 8px 12px, background transparent, border none, color #888, opacity 0.55, font-size 0.85rem, -webkit-tap-highlight-color transparent } and .playback-back:active { opacity 1, color #e0e0e0 }. No display/visibility rules — hiding rides on the existing .playback-controls.hidden container behavior.
  </action>
  <verify>
    <automated>npm run build && test "$(grep -c 'playback-back' src/components/PlaybackControls.tsx)" = "1" && test "$(grep -c 'opacity: 0.55' src/index.css)" = "1" && test "$(grep -c 'onBack' src/components/PlaybackControls.tsx)" -ge "2"</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0 with existing call sites untouched (prop is optional; App.tsx wiring of onBack is 05-04's task)
    - Exactly one playback-back className in the component, rendered only when onBack exists AND status is playing or paused, positioned before the offset controls
    - Label reuses the existing back key (no new i18n key for the back control)
    - CSS values above present; 48×48px touch floor honored (UI-SPEC 2xl token overrides legacy minimums)
  </acceptance_criteria>
  <done>Back control renders as the first control of the playing‖paused branch with the D-09 low-visual-weight treatment and hides together with the existing 3s auto-hide container</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied file name → DOM | The banner renders session.fileName, which originates from an untrusted local file's name. React text-node escaping is the control surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-01 | Tampering / XSS-adjacent | SessionBanner fileName + toast copy rendering | low | accept | React text nodes only (Phase-1 subtitle canon); file contains no raw-HTML injection API — asserted by grep gate in Task 2's acceptance criteria |
| T-05-03-02 | Info disclosure | Banner/toast on-screen content | low | accept | Content is the user's own movie name + elapsed time, shown on their own device screen — no new exposure class |
| T-05-03-SC | Tampering | npm installs | n/a | accept | Zero new dependencies — package.json untouched; slopcheck not triggered |

Documented honestly per phase security calibration: local-only PWA, no backend, no accounts, no PII, no network calls. Subtitle text XSS remains existing OWASP canon (documented in Phase 1) — breadcrumb only, not re-minted here.
</threat_model>

<artifacts_produced>
## Artifacts this plan produces

| Kind | Symbol | Location |
|------|--------|----------|
| component | `SessionBanner` (props: session, status, onResume, onDismiss) | src/components/SessionBanner.tsx |
| component | `SessionToast` (props: trigger) | src/components/SessionToast.tsx |
| prop | `PlaybackControlsProps.onBack` (optional) | src/components/PlaybackControls.tsx |
| i18n key | `resumePlaying` / `resumePlayingAria` / `sessionKeptToast` / `sessionElapsed` / `sessionPausedAt` / `dismissSession` (each in en + zh) | src/i18n/translations.ts |
| CSS | `.session-banner`, `.session-banner-text`, `.session-banner-title`, `.session-banner-meta`, `.session-banner-resume`, `.session-banner-dismiss` (+ `:active`) | src/index.css |
| CSS | `.session-toast`, `.session-toast.hidden` | src/index.css |
| CSS | `.playback-back`, `.playback-back:active` | src/index.css |

Consumers: App.tsx (05-04) mounts SessionBanner/SessionToast atop both selection views and passes onBack into the playback-view PlaybackControls.
</artifacts_produced>

<verification>
- npm run build — typecheck green (also structurally enforces en/zh key parity)
- Per-task grep gates above
- No fill brighter than #222 introduced (cinema luminance rule); all new touch targets ≥48×48px
- Visual/interactive verification of these surfaces against a live session happens in 05-04's device checkpoint (integration context required)
</verification>

<success_criteria>
All three UI surfaces exist per the approved UI-SPEC with locked copy (including the REQUIREMENTS-pinned zh toast string), D-06/D-07/D-08/D-09 visibly implemented, the eight applicable UI-consideration rows honored, and zero dialogs/gated gestures anywhere.
</success_criteria>

<output>
Create `.planning/phases/05-lossless-playback-navigation/05-03-SUMMARY.md` when done. Force-add plan/summary files (`git add -f`) — this repo gitignores .planning and tracks planning artifacts deliberately.
</output>
