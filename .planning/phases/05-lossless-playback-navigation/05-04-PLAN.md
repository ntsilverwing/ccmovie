---
phase: 05-lossless-playback-navigation
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03]
files_modified: [src/playback/PlaybackEngine.ts, src/hooks/usePlaybackEngine.ts, test/unit/playbackEngine.test.ts, src/App.tsx]
autonomous: false
requirements: [PLAY-08]
must_haves:
  truths:
    - "D-01: entering playback registers the {view:'playback'} entry via enterPlaybackHistory, and an Android system back during playback fires popstate → lands on the selection view instead of exiting the PWA; back-control tap and system back converge on ONE leave path (UI-SPEC I-2 identical landing)"
    - "D-02: the wiring pushes/replaces through 05-02's tested policy — pause→resume and banner->resume re-entries never stack entries (replace branch taken because top already carries the marker)"
    - "D-03: selection-page system back is never intercepted — the back-control path consumes the marker entry via history.back(), leaving the selection view at the base of the app stack so the next back exits the app naturally"
    - "D-04: leaving playback performs exactly the double release — exitFullscreen() + Wake Lock disable — and never pauses or stops the engine; the subtitle clock keeps pace with real time while away (sessionElapsedMs continuity from 05-01 makes this provable)"
    - "D-05: re-entering playback re-acquires Wake Lock + fullscreen inside the user tap gesture chain by passing handlePlay itself to the banner (verbatim reuse, PATTERNS §1) — the synchronous enableWakeLock() call precedes the async requestFullscreen request (iOS ordering), and the history write executes before any React state update"
    - "D-08: banner dismiss goes through the existing handleStop path — session abandoned, banner unmounts, selection page returns to its v1.0 composition; single tap, no confirmation"
    - "D-09: the leave path is single-tap end to end; its reversal is one banner tap — no double-tap, long-press, or dialog exists anywhere in leave/resume"
    - "UI-considerations [error]: Phase 5 adds no failure modes — the existing .error-banner import surface is untouched, and fullscreen/wake-lock rejections stay silent via .catch(() => {}) exactly like v1.0's handlePlay"
    - "Auto-end convergence (CONTEXT the agent's discretion #4): engine natural exhaustion fires onEnded → dispatch → status idle → session cleared; an App effect converges view → selection on any idle transition, so the banner unmounts per I-7 and no stuck black/playback screen can exist"
    - "Session invariant: an active session implies its subtitle is still loaded — deselecting the movie from the ready view runs hook stop first (adjacency probe: importing or selecting another subtitle mid-session already runs the existing stop path in handleImport, replacing the session)"
    - "Controls re-show (discretion #3 / I-8): the auto-hide effect's dependency list includes view and re-arms the 3s timer on re-entry; on selection views it keeps controls visible and never arms hiding while status stays 'playing' away from playback"
    - "Edge (resume-at-end): tapping resume at or past the final cue end plays → the first tick hits natural exhaustion → onEnded converges to idle — no crash, no negative display (engine-level behavior asserted in Task 1's new tests)"
  artifacts:
    - path: src/playback/PlaybackEngine.ts
      provides: "edited — optional third constructor arg onEnded fired on internal auto-stop paths (natural exhaustion, cue-empty halt)"
      contains: "onEnded"
    - path: src/hooks/usePlaybackEngine.ts
      provides: "edited — session: PlaybackSession | null exposed alongside state/play/pause/stop; SessionIdentity third parameter"
      exports: [SessionIdentity]
    - path: src/App.tsx
      provides: "view state decoupling ('selection' | 'playback'), popstate interception, leavePlayback double-release, banner/toast mounts, idle-convergence effect"
      contains: "enterPlaybackHistory"
    - path: test/unit/playbackEngine.test.ts
      provides: "appended onEnded cases (fires on natural exhaustion; does NOT fire on explicit stop)"
      contains: "onEnded"
  key_links:
    - from: src/hooks/usePlaybackEngine.ts
      to: src/playback/session.ts
      via: "play/pause/stop transitions call createSession/resumeSession/pauseSession against the current session (05-01 module import)"
      pattern: "from '../playback/session'"
    - from: src/App.tsx
      to: src/playback/playbackHistory.ts
      via: "handlePlay registers the entry; handleBackControl consumes it; popstate effect guards on viewRef"
      pattern: "enterPlaybackHistory\\(window\\.history\\)"
    - from: src/App.tsx
      to: src/components/SessionBanner.tsx
      via: "banner mounted atop both selection views; onResume receives handlePlay (verbatim D-05 chain); onDismiss receives handleStop"
      pattern: "onResume={handlePlay}"
    - from: src/App.tsx
      to: src/hooks/usePlaybackEngine.ts
      via: "third argument activeIdentity { subtitleId, fileName } — subtitleId from the matching saved record id when found, else the file name"
      pattern: "usePlaybackEngine\\(subtitle\\?\\.cues"
  prohibitions:
    - { statement: "The session clock must never be paused, reset, or re-anchored by leaving or re-entering the playback view — leave/resume may only move UI state (view, fullscreen, Wake Lock); engine timing, session.startedAt, and the current offset stay untouched across the transition", status: unverified, flagged: true }
    - { statement: "History interactions at the wiring layer consume only self-created entries: window.history.back() is invoked solely when the top entry passes isPlaybackEntry; the popstate handler is idempotent (no-op when already on selection) so crafted back/forward churn cannot desync view from engine", status: unverified, flagged: true }
---

<objective>
Wire Phase 5's tested pieces (05-01 session math, 05-02 history policy, 05-03 surfaces) into the live app: decouple view from playback status in App.tsx, intercept the Android back gesture via popstate (D-01..D-03), implement the divergence-free leave/re-enter flows (D-04/D-05), give the engine an auto-end signal (onEnded) so sessions converge to idle, and verify the full loop on a real Android PWA.

Purpose: This is where "return without ever losing the synced position" becomes observable behavior — every prior plan feeds this one, and each flow reuses existing proven patterns (PATTERNS §1) rather than new ones.
Output: Engine + hook session wiring with new tests, fully integrated App navigation, and one device-verification checkpoint.
</objective>

<execution_context>
@/home/haonson/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/haonson/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-lossless-playback-navigation/05-CONTEXT.md
@.planning/phases/05-lossless-playback-navigation/05-UI-SPEC.md
@.planning/phases/05-lossless-playback-navigation/05-PATTERNS.md
@src/App.tsx
@src/hooks/usePlaybackEngine.ts
@src/playback/PlaybackEngine.ts
@src/playback/session.ts
@src/playback/playbackHistory.ts
@src/components/SessionBanner.tsx
@src/components/SessionToast.tsx
@src/components/PlaybackControls.tsx
@test/unit/playbackEngine.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine onEnded signal + hook session wiring</name>
  <files>src/playback/PlaybackEngine.ts, src/hooks/usePlaybackEngine.ts, test/unit/playbackEngine.test.ts</files>
  <read_first>
    - test/unit/playbackEngine.test.ts (existing rAF-mocking harness — append cases in its exact idiom; do not introduce new mocking libraries)
    - src/playback/PlaybackEngine.ts lines 84-160 (play guards, internal tick stop branches at cues-empty and natural exhaustion)
    - src/hooks/usePlaybackEngine.ts (whole file — reducer, cuesRef/offset effects, useCallback([]) discipline)
    - src/playback/session.ts (05-01 exports + PlaybackSession type)
  </read_first>
  <behavior>
    - onEnd(cues exhausted): elapsed passes last cue end while playing → engine auto-stops → onEnded callback fires exactly once → hook dispatches STOP and clears session (status → idle convergence, discretion #4)
    - onStop(explicit): hook-driven stop does NOT fire onEnded (session is cleared by hook directly — symmetric, no double dispatch)
    - Session lifecycle: play() with no session + identity present creates one anchored at Date.now(); play() with existing paused session resumes with no elapsed jump; pause() freezes; stop() clears; offsetMs prop changes flow into updateSessionOffset
  </behavior>
  <action>
    1) PlaybackEngine.ts: add optional third constructor parameter onEnded?: () => void stored as a private field. Invoke this.onEnded?.() in tick() on BOTH internal auto-stop paths (the cues-empty halt and the natural-exhaustion branch), immediately AFTER the internal stop call so state is settled before the callback runs. The public stop() method does NOT fire it — the hook's explicit stop path clears session itself. Update the class header JSDoc: the rAF-only discipline stands; onEnded exists so React state can converge to idle (cite discretion #4 from 05-CONTEXT.md).
    2) test/unit/playbackEngine.test.ts: append an onEnded describe block in the existing harness idiom — construct an engine with the third arg, drive it past the last cue end as existing elapsed-based cases do, assert the spy fired once on natural exhaustion, and assert a direct stop() call leaves the spy unfired. Follow the file's existing play/drive mechanisms verbatim.
    3) usePlaybackEngine.ts: export a new interface SessionIdentity { subtitleId: string; fileName: string }; extend the hook signature with optional third parameter identity: SessionIdentity | null = null; add playbackSession state (useState, PlaybackSession | null, init null) returned as session from the hook alongside state/play/pause/stop. Keep play/pause/stop as useCallback([]) by reading live values from NEW refs: identityRef (synced in a useEffect keyed on identity) and offsetMsRef (synced in the existing offset effect — same effect now also applies updateSessionOffset to any live session). play(): setSession(prev => prev ? resumeSession(prev, Date.now()) : (identityRef.current ? createSession({ subtitleId: identityRef.current.subtitleId, fileName: identityRef.current.fileName, offsetMs: offsetMsRef.current, now: Date.now() }) : null)). pause(): setSession(prev => prev ? pauseSession(prev, Date.now()) : prev). stop(): existing body + setSession(null). Engine-creation effect: pass the third constructor arg — a closure that dispatches STOP and calls setSession(null). Do NOT put view state anywhere near this reducer (CONTEXT code_context: view lives at App layer).
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run exits 0 — existing engine/reducer suites unchanged and green, plus the new onEnded cases
    - New tests assert: natural exhaustion fires the spy exactly once; a direct public stop call does not fire it
    - Hook return type includes session: PlaybackSession | null; SessionIdentity exported; identity defaults to null so the build stays green BEFORE Task 2 updates the call site
    - PlaybackEngine constructor gains the optional third parameter; its header JSDoc still contains the rAF-only timing contract (grep -c 'requestAnimationFrame only schedules visual updates' src/playback/PlaybackEngine.ts = 1)
  </acceptance_criteria>
  <done>Engine signals natural/auto halts; hook owns an in-memory PlaybackSession across play/pause/stop; full suite + build green with App.tsx untouched</done>
</task>

<task type="auto">
  <name>Task 2: App.tsx view decoupling + history interception + leave/resume flows</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (whole file — every edit anchor below cites its current line region)
    - src/playback/playbackHistory.ts (05-02 exports + marker contract)
    - src/components/SessionBanner.tsx and src/components/SessionToast.tsx (05-03 props contracts)
    - .planning/phases/05-lossless-playback-navigation/05-PATTERNS.md §1 (imports pattern, listener-lifecycle lines 29-33, gesture-chain lines 93-102, release lines 109-115, auto-hide lines 126-146)
    - .planning/phases/05-lossless-playback-navigation/05-UI-SPEC.md Interaction Contract I-1..I-8
  </read_first>
  <action>
    Edits, in file order (all identifiers concrete; existing unlisted code untouched):
    1) Imports: add SessionBanner, SessionToast from './components/...'; enterPlaybackHistory and isPlaybackEntry from './playback/playbackHistory'. Keep the existing relative-import, no-alias style.
    2) State: add `const [view, setView] = useState<'selection' | 'playback'>('selection')`, `const viewRef = useRef(view)` synced via `useEffect(() => { viewRef.current = view }, [view])`, and `const [lastLeftAt, setLastLeftAt] = useState<number | null>(null)`.
    3) Session identity: derive `const activeIdentity = subtitle ? { subtitleId: savedSubtitles.find((s) => s.fileName === subtitle.metadata.fileName)?.id ?? subtitle.metadata.fileName, fileName: subtitle.metadata.fileName } : null` and pass it as the third argument to usePlaybackEngine; destructure the new session field from the hook's return.
    4) handlePlay (lines 93-102 — extend, gesture chain retained VERBATIM first): unchanged enableWakeLock() synchronous call, unchanged fullscreen request fire-and-forget with silent catch, THEN `enterPlaybackHistory(window.history)` (D-01; replace-vs-push decided by 05-02's tested policy — D-02), then play(), then setView('playback'). Fixed call order — two ordering invariants hold: the synchronous wake call precedes the async fullscreen request (iOS probe), and the history write precedes every React state update (history/react ordering probe). This one handler serves ready-view Start, paused resume, AND the banner resume CTA (D-05 verbatim reuse).
    5) New leavePlayback useCallback (deps [disableWakeLock]): the D-04 double release — disableWakeLock(); if (document.fullscreenElement) document.exitFullscreen?.().catch(() => {}); — then setView('selection') and setLastLeftAt(Date.now()). This handler contains no invocation of the engine's lifecycle methods at all: the clock keeps pacing real time while away (I-2 toast trigger = lastLeftAt change).
    6) New popstate effect in the lines-29-33 listener pattern (deps [leavePlayback]): handler ignores the event when viewRef.current is not 'playback' (idempotent against back/forward churn — crafted sequences cannot desync view from engine); otherwise calls leavePlayback(). addEventListener/removeEventListener on window, cleanup in the effect return.
    7) New handleBackControl useCallback (deps [leavePlayback]): no-op unless viewRef.current === 'playback'; when the current top entry passes isPlaybackEntry(window.history.state), invoke window.history.back() so ONE popstate converges the view (single leave path, D-01/I-2); otherwise leavePlayback() directly (missing-marker edge). Wire as onBack={handleBackControl} ONLY at the playback-view PlaybackControls render site.
    8) Auto-hide effect (lines 126-146): add view to the dependency array and strengthen the early-return guard to also fire when view !== 'playback' — selection views keep controls visible even while a session plays in the background; re-entering playback flips view, re-runs the effect, and re-arms the 3-second timer (I-8, discretion #3) with no other timer changes.
    9) New idle-convergence effect (deps [playbackState.status, view]): when view is 'playback' AND status is 'idle', setView('selection'). This single effect converges in-view natural exhaustion (Task 1's onEnded path), user tap on the existing Stop button, and any future idle transition — no stuck black screens; away-from-playback auto-end is a no-op through it (I-7 banner unmount via session clear).
    10) handleStop (lines 109-115): after the existing release lines, add setView('selection') (view no longer follows status implicitly). It intentionally does NOT touch lastLeftAt — deliberate abandon (Dismiss ×) shows no kept-session toast; the toast is reserved for leave transitions (I-1/I-2).
    11) Ready-view deselect button (lines 226-232): extract onClick to handleDeselectMovie that runs hook stop FIRST, then setSubtitle(null) and setError(null) — the session-invariant guard (a session never outlives its loaded movie; adjacency probe).
    12) Render decoupling: DELETE the status-driven playback-view early-return at lines 148-149 (the hard coupling PATTERNS singles out) and replace it with `if (view === 'playback' && subtitle)` wrapping the same playback tree, adding onBack={handleBackControl} to that tree's PlaybackControls call. In the import-view return, add as the first elements inside the app div: SessionBanner with session={session}, status={playbackState.status}, onResume={handlePlay}, onDismiss={handleStop}, followed by SessionToast trigger={lastLeftAt}. In the ready-view return, mount the same two elements before the existing .back-button (banner mount coverage spans both selection views; the import-view mount is the D-07 seam Phase 6's resume card will light up after relaunch).
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npm run build &amp;&amp; test "$(grep -c "addEventListener('popstate'" src/App.tsx)" = "1" &amp;&amp; test "$(grep -c 'enterPlaybackHistory(window.history)' src/App.tsx)" = "1" &amp;&amp; test "$(grep -c '&lt;SessionBanner' src/App.tsx)" = "2" &amp;&amp; test "$(grep -c '&lt;SessionToast' src/App.tsx)" = "2" &amp;&amp; ! grep -q "playbackState.status === 'playing' || playbackState.status === 'paused'" src/App.tsx &amp;&amp; ! awk '/const leavePlayback = useCallback/,/\}, \[/' src/App.tsx | grep -qE '\b(stop|pause|play)\(' &amp;&amp; L=$(awk '/const handlePlay = useCallback/,/\}, \[/' src/App.tsx | grep -n 'enableWakeLock()' | head -1 | cut -d: -f1) &amp;&amp; F=$(awk '/const handlePlay = useCallback/,/\}, \[/' src/App.tsx | grep -n 'requestFullscreen' | head -1 | cut -d: -f1) &amp;&amp; H=$(awk '/const handlePlay = useCallback/,/\}, \[/' src/App.tsx | grep -n 'enterPlaybackHistory' | head -1 | cut -d: -f1) &amp;&amp; test "$L" -lt "$F" &amp;&amp; test "$F" -lt "$H"</automated>
  </verify>
  <acceptance_criteria>
    - npx vitest run exits 0 and npm run build exits 0
    - Exactly one popstate listener registration; exactly one enterPlaybackHistory(window.history) call site; SessionBanner and SessionToast each mounted at exactly two sites (import view + ready view)
    - The v1.0 status→view coupling condition no longer exists anywhere in App.tsx; rendering branches on view + subtitle only
    - leavePlayback's region (its useCallback body) contains no engine lifecycle invocation — region-scoped negative gate above
    - Ordering probe proven by region grep: within handlePlay, the synchronous enableWakeLock line-number < requestFullscreen line-number < enterPlaybackHistory line-number; history write precedes the play()/setView React updates
    - Idle-convergence effect present: status idle while in playback view flips view to selection
    - handleBackControl consumes the marker only when isPlaybackEntry passes (D-03); system back on selection view has zero interception code
    - git status shows changes confined to src/App.tsx for this task
  </acceptance_criteria>
  <done>Back control and Android system back both land on the selection view (D-01), the engine keeps clocking through the transition (D-04), banner+toast appear while a session lives, resume via gesture chain restores fullscreen+wake at formula position (D-05), dismiss abandons silently, and the whole flow compiles + passes the full suite</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — lossless navigation loop on Android PWA</name>
  <files>src/App.tsx, src/components/SessionBanner.tsx, src/components/SessionToast.tsx</files>
  <read_first>
    - .planning/phases/05-lossless-playback-navigation/05-UI-SPEC.md Interaction Contract I-1..I-8 (the behaviors being confirmed)
  </read_first>
  <action>
    Human verifies the full leave/resume/back-gesture loop on an installed Android PWA. No code changes — this is a verification checkpoint only.
  </action>
  <verify>
    <automated>echo "Human verification required — see how-to-verify steps below"</automated>
  </verify>
  <done>Human confirms: back control and Android system back both land on selection without exiting the app, the banner elapsed value advances with real time while away, one-tap resume restores playback at the formula position with no manual re-sync, and selection-page back exits the app naturally</done>
  <what-built>
    Lossless playback navigation (PLAY-08) fully integrated: low-visual-weight back control, Android system-back interception via history marker (D-01/D-02/D-03), double-release leave that keeps the clock running (D-04), session banner with live elapsed + one-tap resume (D-05/D-06), dismiss-to-abandon (D-08), kept-session toast, auto-end convergence, all bilingual.
  </what-built>
  <how-to-verify>
    On an Android device with the PWA installed (install was verified working per CONTEXT specifics):
    1. Run `npm run build && npm run preview -- --host` on the dev machine and open the app on the Android device (installed PWA, standalone).
    2. Import or select an SRT → tap Start → playback runs; note the current subtitle.
    3. Tap the dimmed ‹ 返回 control (should sit at ~0.55 opacity, first item of the controls) → lands on the selection view; screen is allowed to sleep (Wake Lock released); a top-center toast reading 原字幕已保留，可直接继续播放 appears and fades after ~3.5s without intercepting taps.
    4. A banner at the top shows the movie name (truncating with … only on the title line) + 已播 h:mm:ss advancing every second + a 继续播放 button + a ×.
    5. Wait ~30 seconds (optionally turn the screen off and on) → tap 继续播放 → fullscreen + wake are re-acquired and playback resumes at the position implied by the ~30s of real elapsed time — NO manual re-sync anywhere.
    6. During playback, perform the Android back gesture (or hardware back) → lands on the selection view again (app did NOT exit); toast shows again.
    7. From the selection view, press system back → the app exits naturally (no toast, no interception).
    8. Re-open the app → fresh launch shows the v1.0 selection composition with NO banner and NO toast (in-memory session is Phase 5 scope; durability is Phase 6).
    9. Rebuild the session (select the movie → Start → back) → tap the banner × → banner disappears with a single tap and no dialog; the page is byte-identical to v1.0's selection state.
    10. Switch language to EN and confirm the toast/banner/back strings render in English with identical layout.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS back gesture → popstate event | Host-controlled history churn (gestures, task-switcher kills, forward/back racing) is untrusted input at the app's single interception point. |
| App → Fullscreen / Wake Lock APIs | Browser may reject either call; rejection paths must not crash or alter playback behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-01 | Tampering | popstate/back-forward sequence handling (App.tsx) | medium | mitigate | Idempotent guard: handler acts only when viewRef says 'playback'; entry consumption gated by isPlaybackEntry (05-02 tested policy); leavePlayback is re-entrant-safe (setView/setLastLeftAt are idempotent state writes) — crafted back/forward churn at worst re-shows a truthful banner |
| T-05-04-02 | Elevation of privilege | Fullscreen / Wake Lock acquisition | low | accept | Gestured-only reacquisition, silent .catch(() => {}) on rejection (v1.0 canon) — no capability persists; worst case is screen sleeps, clock continues |
| T-05-04-03 | Info disclosure | Session object in memory | low | accept | subtitleId/fileName/timestamps only — no PII, process-lifetime only, no persistence in Phase 5 (IndexedDB semantics are Phase 6's threat model) |
| T-05-04-04 | DoS | 1000ms banner interval + rAF loop while away | low | accept | Two already-running loops at v1.0 cost; interval cleared on unmount/pause (05-03 cleanup gate) |
| T-05-SC | Tampering | npm installs | n/a | accept | Zero new dependencies — package.json untouched; slopcheck not triggered |

ASVS L1 calibration per phase security requirement: local-only PWA, no backend, no accounts, no PII, no network beyond SW cache. Subtitle text XSS remains Phase-1 canon (React text nodes) — no new sink introduced here (banner renders via the 05-03 component already gated). No high-severity threats identified; nothing blocks on this register.
</threat_model>

<artifacts_produced>
## Artifacts this plan produces

| Kind | Symbol | Location |
|------|--------|----------|
| constructor param | `PlaybackEngine(cues, onCueChange, onEnded?)` | src/playback/PlaybackEngine.ts |
| interface | `SessionIdentity` ({ subtitleId, fileName }) | src/hooks/usePlaybackEngine.ts |
| hook return | `session: PlaybackSession \| null` | src/hooks/usePlaybackEngine.ts |
| hook param | third arg `identity` (optional) | src/hooks/usePlaybackEngine.ts |
| App state | `view` / `viewRef` / `lastLeftAt` (internal) | src/App.tsx |
| App handlers | `leavePlayback` (D-04), `handleBackControl` (D-01/D-03), `handleDeselectMovie` (session invariant) (internal) | src/App.tsx |
| App effect | popstate listener (D-01), idle-convergence, view-aware auto-hide (I-8) (internal) | src/App.tsx |
| tests | onEnded describe block | test/unit/playbackEngine.test.ts |

## Phase-5 consolidated symbol inventory (all plans)

- src/playback/session.ts — `PlaybackSession`, `createSession`, `pauseSession`, `resumeSession`, `updateSessionOffset`, `sessionElapsedMs`, `formatElapsedHMS`
- src/playback/playbackHistory.ts — `HistoryLike`, `PLAYBACK_ENTRY`, `isPlaybackEntry`, `enterPlaybackHistory`, `exitPlaybackHistory`
- src/components/SessionBanner.tsx — `SessionBanner` (`.session-banner` class family)
- src/components/SessionToast.tsx — `SessionToast` (`.session-toast`)
- src/components/PlaybackControls.tsx — `onBack` prop (`.playback-back`)
- src/hooks/usePlaybackEngine.ts — `SessionIdentity`, `session` return field
- src/playback/PlaybackEngine.ts — `onEnded` constructor param
- src/i18n/translations.ts — `resumePlaying`, `resumePlayingAria`, `sessionKeptToast`, `sessionElapsed`, `sessionPausedAt`, `dismissSession` (en + zh)
- src/index.css — `.session-banner`, `.session-banner-text`, `.session-banner-title`, `.session-banner-meta`, `.session-banner-resume`, `.session-banner-dismiss`, `.session-toast`, `.session-toast.hidden`, `.playback-back`, `.playback-back:active`
- tests — test/unit/session.test.ts, test/unit/playbackHistory.test.ts, onEnded block in test/unit/playbackEngine.test.ts
- App-level internals — `view` state, `leavePlayback`, `handleBackControl`, `handleDeselectMovie`

Phase-6 seams (D-07): `PlaybackSession` shape == persist target; `.session-banner` mount points already span both selection views; `SessionIdentity.subtitleId` pre-links to the IndexedDB record id.
</artifacts_produced>

<verification>
- npx vitest run — full suite green including new onEnded cases
- npm run build — typecheck green
- Region-scoped ordering/purity gates above pass on src/App.tsx
- Device checkpoint (Task 3) approved by the human on an installed Android PWA
</verification>

<success_criteria>
PLAY-08's five behavior clauses are true on-device: low-weight back control lands on selection without accidental taps; Android system back during playback is intercepted to selection; the toast + banner confirm the retained session with elapsed ticking in real time; resume lands at now − startedAt + offset with no manual re-sync; selection-page back exits the app naturally. All D-01..D-09 decisions behave as locked, both ordering probes hold in source, and the auto-end/import adjacency cases converge without artifacts.
</success_criteria>

<output>
Create `.planning/phases/05-lossless-playback-navigation/05-04-SUMMARY.md` when done. Force-add plan/summary files (`git add -f`) — this repo gitignores .planning and tracks planning artifacts deliberately.
</output>
